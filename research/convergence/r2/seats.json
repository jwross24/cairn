{"mode":"review","models":[{"model":"openrouter:openai/gpt-5.6-terra","ok":true,"content":"## 1. Break the Tier-2 ladder admission circularity  \n**Severity:** HIGH  \n**Section:** §§5–6, §13 M1  \n**Component improved:** Tier gate / small-scale ladder admission contract.\n\n**Rationale:**  \nThe current plan makes a Tier-2 algorithmic launch require a Tier-1 ladder artifact with `verdict KEEP` (§5). But the ladder artifact’s `KEEP` verdict includes the 60-bit completion-and-extrapolation rung (§6), and that rung is explicitly Tier-2 work requiring tier-gate approval. Therefore the system needs a Tier-2 ticket before it can generate the Tier-2 evidence required for that ticket.\n\nThis is not merely implementation ambiguity: a strict implementation correctly refuses every 60-bit ladder launch. The fix is to distinguish:\n\n1. a **Tier-2 validation admission ticket**, earned from the ≤50-bit ladder; from  \n2. a **Tier-2 research ticket**, earned only after the 60-bit hold-out result passes.\n\nThis preserves cheap-before-expensive. It permits only the narrowly scoped 60-bit validation run—not arbitrary Tier-2 research—before the full KEEP result exists.\n\n**Cost / failure surface:** One additional ticket type and a narrow-purpose constraint in the tier gate. The main failure risk is accidentally using validation admission as general Tier-2 authorization; prevent this mechanically with an allowed-purpose field.\n\n**Evidence:** **PROVEN-in-source** as a dependency-graph correction: the current stated predicates form a cycle. The relevant mechanism evidence is the existing tier/ladder material in `research/PROPOSALS.md`; this revision does not add a research claim.\n\n**Invariant check:** Does not alter gates, permit strategy to edit truth, or weaken calibration. It makes the existing tier gate executable.\n\n```diff\ndiff --git a/plan-r1.md b/plan-r1.md\n@@ §5 Tier gate predicate\n- for Tier 2 on an algorithmic claim the ticket is a ladder result table with\n- verdict KEEP (§6)\n+ for Tier 2 on an algorithmic claim there are two distinct admission modes:\n+ (a) `ladder_validation_60`: a narrowly scoped Tier-2 launch whose only permitted\n+ purpose is the gate-owned 60-bit hold-out rung; its ticket is an admissible ≤50-bit\n+ ladder table with a pre-registered model, completed A/A null arm, successful recovery\n+ on every completed trial, and a provisional verdict `ELIGIBLE_FOR_HOLDOUT`; and\n+ (b) `algorithmic_research`: any other Tier-2 algorithmic launch; its ticket is the\n+ complete ladder artifact with verdict KEEP, including the 60-bit hold-out result.\n+ The ticket records its permitted purpose, and the tier gate rejects a skill launch\n+ whose declared purpose is not exactly that purpose.\n\n@@ §6 Protocol (2b)\n- The 60-bit rung is the completion-and-extrapolation rung...\n+ The 60-bit rung is the completion-and-extrapolation rung. It may be launched only\n+ under the `ladder_validation_60` admission mode of §5; that admission authorizes no\n+ other Tier-2 work. The completed 60-bit artifact is then incorporated into the\n+ complete ladder table, whose KEEP verdict is required for subsequent Tier-2\n+ algorithmic research.\n\n@@ §13 M1 Done when\n- Adds the ladder protocol (§6) ...\n+ Adds the ladder protocol (§6), including a planted test that a ≤50-bit\n+ `ELIGIBLE_FOR_HOLDOUT` ticket authorizes exactly one gate-owned 60-bit hold-out\n+ protocol and is rejected for every other Tier-2 skill or parameter sweep ...\n```\n\n---\n\n## 2. Do not treat a finite-size loss as an asymptotic refutation  \n**Severity:** HIGH  \n**Section:** §§4, 6, 11  \n**Component improved:** Dead-end ledger / ladder verdict semantics.\n\n**Rationale:**  \nThe plan correctly says finite sizes do not establish an asymptotic (§6), but then says that an attack unable to beat BSGS at 50 bits “is refuted on the spot.” Those statements conflict for any hypothesis whose declared crossover is above 50 bits, or whose claimed advantage is asymptotic rather than a claimed 50-bit performance result.\n\nA measured ladder run can permanently refute only the exact finite-domain hypothesis that was measured—consistent with §4’s otherwise strong rule that a broader blacklist needs formal evidence. A failed 50-bit performance threshold is valuable negative evidence, but must produce:\n\n- `REFUTED` when the pre-registered hypothesis explicitly predicts passing that threshold at that domain; or\n- `REJECT` for the requested promotion / `PARKED` with an explicit crossover or model-validation blocker when it does not.\n\nOtherwise the ledger will incorrectly blacklist hypotheses that the plan itself admits have not been disproved.\n\n**Cost / failure surface:** More precise hypothesis declarations: predicted finite-size performance and crossover must be explicit. This prevents workers from retroactively claiming an unspecified later crossover.\n\n**Evidence:** **PROVEN-in-source** as a logical consequence of the plan’s own distinction between finite measurements and formal/asymptotic claims; see the existing ladder and ledger evidence records in `research/PROPOSALS.md`.\n\n**Invariant check:** Strengthens the permanent-REFUTED boundary; does not weaken the ladder or permit evidence inflation.\n\n```diff\ndiff --git a/plan-r1.md b/plan-r1.md\n@@ §3 Hypothesis key\n- {target family, claimed property or cost model (exponent, constant, crossover ...\n+ {target family, claimed property or cost model (exponent, constant, declared\n+ finite-size predictions and crossover, if any) ...\n\n@@ §4 Ledger preflight\n- A measured refutation reaches exactly the hypothesis object it was measured on ...\n+ A measured refutation reaches exactly the hypothesis object it was measured on,\n+ including its declared finite-size prediction. A failure at size s permanently\n+ REFUTES an algorithmic hypothesis only if that hypothesis predicted the failed\n+ threshold at s (within its pre-registered decision band). Failure of a hypothesis\n+ that declares its crossover above s is evidence against its model fit and blocks\n+ promotion, but is not an asymptotic blacklist; it is recorded as REJECT for the\n+ requested ticket or PARKED with a model-validation blocker. A broader blacklist\n+ needs a formal entry.\n\n@@ §6\n- A \"subexponential attack\" that can't beat baby-step-giant-step at 50 bits is\n- refuted on the spot.\n+ A method whose pre-registered 50-bit prediction requires beating the stated\n+ BSGS floor and fails it is refuted on that measured hypothesis on the spot. A\n+ method that did not predict a 50-bit advantage is not thereby asymptotically\n+ refuted: it fails to earn a ladder KEEP verdict and must carry an explicit,\n+ pre-registered crossover claim for any later reconsideration.\n\n@@ §11 Negative-results map\n- \"approach X provably fails on prime-field curves, here's why\"\n+ \"approach X provably fails on prime-field curves, here's why,\" or\n+ \"the pre-registered finite-domain performance hypothesis H failed on declared\n+ family F and sizes S, here's the result\"\n```\n\n---\n\n## 3. Replace the rho-relative memory band with an explicit Pareto policy  \n**Severity:** MED  \n**Section:** §6  \n**Component improved:** Ladder baseline comparison and acceptance metric.\n\n**Rationale:**  \nThe current acceptance condition requires an operations speedup over O(1)-memory rho **and** says memory must “stay inside the same band.” Taken literally, any nontrivial-memory method fails regardless of its operations improvement, because rho’s memory baseline is constant. That makes the plan internally inconsistent with its BSGS comparison and with research on explicit time-memory tradeoffs.\n\nThe ladder should still reject “faster only by hiding an impractical memory explosion,” but it needs an operational resource policy. Compare work and memory separately, record both, and require declared Pareto improvement under a gate-owned memory budget. A method may qualify if it improves group operations without exceeding a fixed ladder memory cap, or if it improves a declared composite resource budget that is fixed before the run. It must never convert seconds into claimed group-operation gains.\n\n**Cost / failure surface:** Requires a deterministic memory-accounting definition: peak resident memory alone is insufficient for distributed or external-table algorithms. Start at M1 with peak allocated algorithmic table bytes plus explicit spill/storage bytes; report RSS as diagnostic. This slightly broadens testing but prevents accidental exclusion of an important class of methods.\n\n**Evidence:** **STRONG-EMPIRICAL-in-source**—the plan already recognizes BSGS/rho as distinct time-memory baselines; the relevant supporting material should be cited from the ladder/baseline entries in `research/PROPOSALS.md`.\n\n**Invariant check:** Keeps the ladder’s fixed baseline, pre-registration, null control, and stricter-than-self-reporting gate behavior. No statistical score promotes a claim.\n\n```diff\ndiff --git a/plan-r1.md b/plan-r1.md\n@@ §6 Protocol (2)\n- a speedup is KEPT only if the whole claim CI clears `1 + 2·radius` (floor 1%) on\n- group operations *and* memory stays inside the same band\n+ a speedup is KEPT only if the whole claim CI clears `1 + 2·radius` (floor 1%) on\n+ group operations and the method satisfies the gate-owned memory policy. The policy\n+ is pre-registered in the ladder plan as: (i) a peak algorithmic-storage cap for the\n+ rung, and (ii) one of `rho-Pareto` or `declared-resource-budget`. `rho-Pareto`\n+ requires lower group operations than rho without exceeding the cap; it does not\n+ require O(1) memory. `declared-resource-budget` is permitted only for a claim whose\n+ hypothesis pre-registers the work/storage tradeoff and its intended operating\n+ regime. A method that exceeds its cap is REJECT regardless of elapsed time.\n\n@@ §6 Protocol (3)\n- It reports group operations against `√n`, never seconds.\n+ It reports group operations against `√n`, never seconds, and separately reports\n+ peak algorithmic table/storage bytes, explicit external spill bytes, and diagnostic\n+ RSS. The ladder plan defines how each baseline skill accounts for these quantities;\n+ an unaccounted storage channel fails closed.\n```\n\n---\n\n## 4. Make the skeptic’s required checklist available without exposing prover artifacts  \n**Severity:** MED  \n**Section:** §§4, 7  \n**Component improved:** Independent-skeptic dispatch contract.\n\n**Rationale:**  \nThere is a direct operational contradiction. §4 says workers have no gate rubric or detection logic reachable. §7 then says the Skeptic’s rubric includes a required quantifier/domain checklist. A skeptic cannot reliably perform a checklist it cannot see.\n\nThe independence requirement is from the **prover’s reasoning and outputs**, not from a fixed, gate-owned review protocol. The dispatch should supply a read-only skeptic-task template containing the statement and the fixed checklist, while continuing to exclude proof artifacts, panel feedback, prior reviews, gate outcomes, and general substrate access. Since the skeptic cannot promote a claim and its result is reviewed under disagreement protocol, this does not create a route to game promotion.\n\n**Cost / failure surface:** One role-specific immutable dispatch template and tests that its closure excludes prohibited artifacts. The principal risk is template drift; pin it in the gate bundle already defined by the plan.\n\n**Evidence:** **PROVEN-in-source** as a direct consistency repair between the plan’s stated required rubric and its worker-isolation model. No new trust mechanism is introduced.\n\n**Invariant check:** Does not let a worker alter a gate; it gives the skeptic only the gate-owned procedure needed to execute its assigned review.\n\n```diff\ndiff --git a/plan-r1.md b/plan-r1.md\n@@ §4 Workers\n- in whose context no gate rubric or detection logic is reachable.\n+ in whose context no gate rubric or detection logic is reachable, except that a\n+ role-specific, gate-owned task template may include the fixed procedure required for\n+ that role to perform its review. Such a template is read-only, pinned in the gate\n+ bundle, and contains no claim-specific prior outcomes.\n\n@@ §7 Independent skeptic\n- sees only the *statement*, never the prover's reasoning\n+ sees only the *statement* and the fixed gate-owned skeptic checklist, never the\n+ prover's reasoning, proof artifact, prior reviewer output, panel feedback, or gate\n+ outcome. The checklist is a pinned dispatch-template artifact in the gate bundle; it\n+ is not retrieved from the mutable substrate and cannot be claim-specifically edited.\n```\n\n---\n\n## 5. Anchor the append-only log outside the orchestrator’s writable database  \n**Severity:** HIGH  \n**Section:** §3  \n**Component improved:** Substrate history-integrity contract.\n\n**Rationale:**  \nA hash chain with compare-and-swap detects alteration of retained records, but it does not by itself detect truncation or rollback when the orchestrator can write the SQLite database. An attacker/process can replace the current head with an earlier valid head and delete later rows; re-verification “from origin” still succeeds. Therefore the statement “an orchestrator that rewrites history produces a log that fails verification” currently overclaims its protection.\n\nAdd monotonic, externally durable checkpointing of the chain head. For M0 this can be deliberately simple: a human/operator-controlled signed checkpoint file or append-only remote object store, with the deployment recording the expected prior checkpoint. On startup and before promotion, the gate verifies that the local chain extends the latest anchored head. Forks, rollback, and missing anchors fail closed for promotion.\n\n**Cost / failure surface:** Adds key management or a remote append-only storage dependency and an availability failure mode. Mitigate by making inability to contact the anchor block promotion/high-tier spending, not Tier-0 local experimentation; queue signed checkpoints for later reconciliation but do not claim anchored history until reconciled.\n\n**Evidence:** **STRONG-EMPIRICAL-in-source** for append-only checkpointing as the necessary complement to a writable hash chain; the existing hash-chain mechanism entry in `research/PROPOSALS.md` should be extended to distinguish modification detection from rollback detection.\n\n**Invariant check:** Strengthens immutable epistemics and provenance. It does not change the strategy layer or any admission gate’s meaning.\n\n```diff\ndiff --git a/plan-r1.md b/plan-r1.md\n@@ §3 Retention / Mechanism\n- an orchestrator that rewrites history produces a log that fails verification.\n+ modification of a retained record produces a log that fails verification. Because a\n+ writable local hash chain alone cannot detect deletion or rollback to an earlier valid\n+ head, the chain head is periodically anchored outside the orchestrator's write\n+ authority. An anchor records `{chain_head, sequence, timestamp, gate_bundle_hash}` in\n+ an operator-controlled signed checkpoint or append-only remote store. At startup and\n+ before promotion or higher-tier spend, the gate requires the local chain to extend the\n+ latest reachable anchor; rollback, a fork, or an unanchored gap yields inadmissible\n+ for those actions. M0 may use a signed operator-controlled checkpoint file; its\n+ availability policy is explicit rather than silently treating local SQLite as an\n+ append-only authority.\n```\n\n---\n\n### Sections that are sound and need no revision\n- The immutable-epistemics framing in §0 is appropriately load-bearing.\n- The attempt-versus-recipe distinction, `DISAGREE` handling, and cache semantics in §§2–3 are unusually well specified.\n- The claim-statement identity and derived-tag design in §§4 and 7 are strong.\n- The waiver rule correctly prevents an operational waiver from becoming an epistemic pass.\n- §14’s baseline is correctly framed and should not be softened.\n\n### The three proposals I would stake the most on\n1. **Tier-2 ladder admission split** — the present dependency cycle can halt the intended build.\n2. **Finite-size failure versus asymptotic refutation** — essential to keeping the permanent ledger honest.\n3. **External anchoring for the hash-chain head** — necessary for the plan’s claimed history-integrity property under its own threat model.\n\n### Framing concern\nThe only framing error is localized but important: §6 occasionally presents the ladder as if it can “refute” an algorithmic idea in the broad mathematical sense. The plan’s better framing appears elsewhere: ladder results refute exactly pre-registered empirical hypotheses on declared families and ranges; formal no-go results refute broader mathematical claims. Preserve that distinction everywhere, especially in user-facing negative-results artifacts.\n\n### Facts needed but not stated\n1. **Threat model for the orchestrator host:** Can the orchestrator modify deployment configuration, database files, process environment, or only ordinary substrate rows? This determines whether external anchoring is mandatory at M0 or merely before multi-user/high-value operation.\n2. **Definition of algorithmic memory:** Does “memory” include external storage, networked distinguished-point tables, recomputation caches, and worker-process memory? The ladder cannot enforce a memory condition without this.\n3. **Tier-2 authorization granularity:** Can the tier gate constrain a launch to a protocol/purpose and inspect the actual skill parameters, or does it currently only inspect a declared tier? Proposal 1 requires typed purpose binding.\n4. **Whether finite-domain performance is part of every algorithmic hypothesis:** If not, the hypothesis schema needs an explicit nullable field rather than treating absence as an implied claim.","ms":71340},{"model":"openrouter:x-ai/grok-4.5","ok":true,"content":"# Plan review (plan-r1.md)\n\nThe spine is sound: §0, gate bundle immutability, derived tags, substrate attempts-not-slots, ladder-as-anti-fabrication, and M0-first build order cohere. Below are only changes that fix contracts, bypasses, or untestable done-whens. Several large sections (§0–2, §8–9, §14, §16) need nothing structural.\n\n---\n\n### 1. Split ladder verdicts — break Tier-2 ↔ KEEP deadlock  \n**Severity:** HIGH · **§5, §6**\n\n**Rationale:** KEEP requires the 60-bit hold-out (§6.2b). That rung is Tier-2 by cost. The Tier-2 ticket for an algorithmic claim *is* a KEEP table (§5). A fresh implementer cannot admit the hold-out run without already having KEEP, and cannot get KEEP without the hold-out. The ≤50-bit distribution work is the real cheap gate; 60-bit is extrapolation under budget, not the admission ticket.\n\n**Diff:**\n```diff\n--- a/plan §5 Tier gate predicate\n+++ b/plan §5 Tier gate predicate\n- for Tier 2 on an algorithmic claim the ticket is a ladder result table with\n- verdict KEEP (§6);\n+ for Tier 2 on an algorithmic claim the ticket is a ladder result table with\n+ verdict KEEP-PROVISIONAL (§6) — ≤50-bit distribution rungs + A/A band only;\n+ Tier 3 (and STRONG-EMPIRICAL promotion of an asymptotic cost model) require\n+ full KEEP, which adds the 60-bit hold-out under an already-granted Tier-2 budget;\n\n--- a/plan §6 Protocol (2)/(2b)/(5)\n+++ b/plan §6 Protocol (2)/(2b)/(5)\n+ Verdicts (fixed in the ladder plan, gate bundle):\n+ - REJECT / INCONCLUSIVE — as today on the ≤50-bit band and refutation floor.\n+ - KEEP-PROVISIONAL — ≤50-bit CI clears the rho band and memory band; method\n+   recovers x on all required trials. This is the Tier-2 admission ticket only.\n+   Tag ceiling remains CONJECTURE until hold-out (or explicit non-asymptotic scope).\n+ - KEEP — KEEP-PROVISIONAL plus 60-bit completion (≥10 fresh recoveries) and\n+   in-band out-of-sample prediction of the pre-registered model. Required for\n+   STRONG-EMPIRICAL on the cost model and for any Tier-3 request built on it.\n+ The 60-bit rung is never itself a Tier-1 self-ticket; it runs only with Tier-2\n+ budget granted on KEEP-PROVISIONAL.\n```\n\n---\n\n### 2. Enforce cost profiles at runtime (close tier-gate bypass)  \n**Severity:** HIGH · **§5 (and pointer in §4 gate layer)**\n\n**Rationale:** The tier gate admits on *declared* profile + ticket. Measured overspend is only a strategy alarm (P4), “never a gate.” A skill that declares Tier-1 and runs Tier-2 wall-time/core-hours skips the ticket that “almost nothing” is supposed to skip—the exact failure cheap-before-expensive is meant to stop. Declaration must be a binding ceiling with a mechanical breach status, not an estimate honored on the honor system.\n\n**Diff:**\n```diff\n--- a/plan §5 Mechanism / Tier gate\n+++ b/plan §5 Mechanism / Tier gate\n  Measured cost is compared against the declared profile; a persistent mismatch\n- is a strategy-layer alarm (§15 P4), never a gate.\n+ is a strategy-layer alarm (§15 P4). Separately, every launch carries a hard\n+ spend ceiling = declared profile × slack (slack ∈ gate bundle, default 2×\n+ time and 2× peak memory). The harness meters the worker (cgroup/process\n+ budget or equivalent). Breach ⇒ kill; attempt status = BUDGET_EXCEEDED;\n+ status ≠ OK ⇒ never cached; output is inadmissible as a tier ticket or as\n+ justify() evidence. Under-declaring cannot purchase a higher tier. P4 alarms\n+ still fire on chronic near-ceiling spend that does not breach.\n+ *Why:* admission-time tier checks on self-declared cost are fail-open under\n+ \"never give up\" pressure; the ceiling makes the declaration a gate input.\n```\n\n**Adoption bar:** improves tier gate + substrate attempt integrity. Cost: OS metering, one status enum, bundle constant. Evidence: CONJECTURE (standard resource quarantine; no need for e-values). Does not edit truth or dissolve gates—adds enforcement.\n\n---\n\n### 3. M1 corpus needs positive controls  \n**Severity:** HIGH · **§13 M1**\n\n**Rationale:** Done-when requires “no matched true advance is rejected,” but (a)–(i) are all planted failures. A harness that rejects everything passes every negative fixture and fails the true-advance clause with no failing test. Same shape as a gate without a must-PASS fixture (§4).\n\n**Diff:**\n```diff\n--- a/plan §13 M1 Done when\n+++ b/plan §13 M1 Done when\n  every fixture in the M1 planted corpus is caught under a cold cache\n  (`skip_cache_lookup`), INCONCLUSIVE counting as an escape, and no matched true\n  advance is rejected; minimum corpus, ≥ 30 plantings across it: (a)…(i)…\n+ ; plus ≥ 6 positive controls that must PASS (not merely \"not reject\"):\n+ (j) plain rho_dp on gate instances at 30/40-bit with pre-registered ~1.25√n\n+     model → KEEP-PROVISIONAL (or tier-appropriate pass) and xP==Q;\n+ (k) bsgs meeting its known cost band on the same instance stream;\n+ (l) a minimal Lean Solution of a Challenge with exact statement match, axioms\n+     ⊆ allowed set, no sorryAx;\n+ (m) a reproducible Tier-1 measurement with repro node admitted to CONJECTURE\n+     or STRONG-EMPIRICAL per justify() rules;\n+ (n) a deliberate counterexample hunt that correctly kills a false small-numbers\n+     pattern (hunt succeeds = gate pass);\n+ (o) a null no-go declaration path on a non-target subproblem that must not\n+     inflate scrutiny.\n+ Scores reported as (caught plantings / plantings) and (passed controls /\n+ controls); either ratio below 1.0 fails M1.\n```\n\n---\n\n### 4. Ledger preflight: range-subset and method-identity closure  \n**Severity:** MED · **§4 ledger / §3 hypothesis key**\n\n**Rationale:** Hypothesis keys drop free text (good) but hash declared parameter ranges. Widening/narrowing ranges or renaming method parameters mints a new key and re-funds work REFUTED on a superset/subset family. Near-dup SimHash is advisory and text-based, so it misses structured range games. Without a structural preflight, REFUTED teeth are cosmetic under orchestrator pressure to keep moving.\n\n**Diff:**\n```diff\n--- a/plan §4 Ledger preflight\n+++ b/plan §4 Ledger preflight\n  `Blocked` = the hypothesis key (§3) matches a REFUTED entry whose retry\n  predicate is unmet;\n+ OR an existing REFUTED entry with the same target family + method identity\n+ whose declared parameter region *covers* this proposal's region (axis-aligned\n+ range inclusion on numeric params; equality on categorical params) and whose\n+ retry predicate is unmet — subset re-probes do not mint a clean key;\n+ OR same method identity + overlapping region with only a cost-model constant\n+ tweak and no new falsifiable claim field (routes `RequiresNullControl` or\n+ human review, not silent Allowed).\n  `Allowed` = no match, or a match whose retry predicate is met…\n+ Coverage edges are recorded on the new branch when Allowed via retry.\n```\n\nDoes not use embeddings or majority vote; pure typed inclusion on the object already hashed.\n\n---\n\n### 5. Ladder commit–reveal as a gate step  \n**Severity:** MED · **§6 Protocol (0)–(1)**\n\n**Rationale:** “Entropy the worker never sees before committing its method” is the right anti-overfit rule but is not a checkable step sequence. Implementers will pass seeds in the same dispatch record. Make commit–reveal substrate-visible like other gates.\n\n**Diff:**\n```diff\n--- a/plan §6 Protocol (0)(1)\n+++ b/plan §6 Protocol (0)(1)\n  (0) The claim pre-registers its cost model … before any rung runs;\n+     the hypothesis/claim statement hash is written to the substrate and named\n+     in the ladder dispatch record *before* step (1); the ladder refuses to\n+     start if the commit is missing or differs from the funded branch hypothesis.\n  (1) The ladder generates its own instances…\n+     Instance-maker entropy is drawn only after the commit receipt exists;\n+     the instance stream seed is gate-owned, stored in the gate-run record, and\n+     absent from the worker tool allow-list. A worker-supplied instance list is\n+     a planted-failure fixture that must FAIL the ladder gate self-test.\n```\n\n---\n\n### 6. Name the conjecture Tier-2 ticket artifact  \n**Severity:** MED · **§5, §7 strong-law**\n\n**Rationale:** Algorithmic Tier-2 tickets have a concrete table shape. Conjecture path says only “counterexample-hunt record of §7,” with no fields, trial counts, or status. That is not implementable at the same rigor as the ladder ticket and invites ad-hoc orchestrator PDFs.\n\n**Diff:**\n```diff\n--- a/plan §5 Tier gate / §7 strong law\n+++ b/plan §7 (strong law) + §5 pointer\n+ Counterexample-hunt record (substrate node, Tier-2 ticket for conjecture-class\n+ branches): {statement hash, sampling distribution D (content-addressed),\n+ pre-registered hunt budget (n, family bounds), seed commit receipt, per-trial\n+ outcomes, any counterexample certificate with verifier status, verdict ∈\n+ {SURVIVED, KILLED, BUDGET_EXHAUSTED_INCONCLUSIVE}, A/A or scramble null when\n+ the claim is distributional}. SURVIVED under the declared budget is the ticket;\n+ KILLED writes REFUTED/PARKED per ledger rules; INCONCLUSIVE is not a ticket.\n+ Tag ceiling without this node: CONJECTURE at most; never STRONG-EMPIRICAL.\n```\n\n---\n\n### 7. M4 store concurrency contract (no new DB)  \n**Severity:** MED · **§3 Store, §13 M4**\n\n**Rationale:** M4 “parallel tracks” on a single SQLite substrate without a writer contract will lose attempts or interleave chain heads. Plan already rejected franken-DBs and Temporal; still need a boring concurrency rule before scale-out is “done.”\n\n**Diff:**\n```diff\n--- a/plan §3 Store / §13 M4\n+++ b/plan §3 Store\n+ Concurrency (M0–M3): one writer connection, WAL mode, busy-timeout; workers\n+ are subprocesses that return artifacts to the harness writer — they do not\n+ hold substrate write capabilities (mirrors gate-bundle read-only discipline).\n+ M4 done-when adds: N≥4 parallel tracks under that contract with zero broken\n+ hash-chain heads and zero lost attempt rows in the M4 stress fixture; if the\n+ fixture fails, the fix is queue depth / batching / multi-process write proxy,\n+ not a new database dependency.\n```\n\n---\n\n### 8. Skill self-test failure ⇒ automatic yank  \n**Severity:** LOW–MED · **§2**\n\n**Rationale:** Floor + golden certificate are defined; the consumer on regression is not. A skill that fails its corpus mid-campaign should not keep minting OK tickets until a human notices.\n\n**Diff:**\n```diff\n--- a/plan §2 Skill identity\n+++ b/plan §2\n+ On self-test failure (floor miss, certificate mismatch, double-run divergence):\n+ skill revision is YANKED — new launches refused, in-flight attempts finish as\n+ AuditOnly/inadmissible, salt optional for poison namespace, ledger alarm. Only an\n+ attributed new revision with a fresh certificate may clear the yank. *Why:*\n+ without yank, §2 is documentation; with it, skill identity is a live gate input.\n```\n\n---\n\n### 9. Clarity only — don’t advertise info-per-dollar as current  \n**Severity:** LOW · **§5**\n\n**Rationale:** Body states allocation currency *is* expected information per dollar, then says until M3 it is gate-outcome/cost. Readers will implement a fictional info estimator.\n\n**Diff:**\n```diff\n- **Allocation currency is expected information per compute-dollar**, not raw yield\n- — …; until M3 decides its mechanics (§15 P3) it is implemented as gate-outcome\n- reward per measured cost…\n+ **Allocation target (M3+):** expected information per compute-dollar, not raw\n+ yield. **M0–M2 / default until P3 decides:** gate-outcome reward per measured\n+ cost only; no model probability, e-value, posterior, or similarity term.\n```\n\n---\n\n### Sound as written (no change)\n§0; §1; §8 no-go presence/consumer split; §9 persistence vs honesty; §10 human-absent = block; §12 borrow-mechanisms-not-code; §14 baseline; §15 P1–P7 deferral shape; §7 derived tags / e-values never promote; challenge/solution Lean protocol; attempt-append substrate.\n\n---\n\n### (a) Three to stake on\n1. **#1 KEEP-PROVISIONAL vs KEEP** — without it M1/M2 ladder work cannot lawfully reach its own hold-out.  \n2. **#2 runtime budget ceiling** — otherwise declared tiers are advisory under the exact pressure model §0 assumes.  \n3. **#3 M1 positive controls** — otherwise M1 “done” can ship a universal rejector.\n\n### (b) Framing concerns\n- **Progress = gate outcomes** (until M3) will bias toward *easy, frequent gate passes* (toy refutations, trivial lemmas) unless problem-queue taste (#10) is truly in the loop early. Not wrong, but M3’s bar should explicitly include “does not starve slow high-payoff branches.” The plan underweights that failure mode vs stall-withdrawal.  \n- **Ladder is ECDLP-shaped.** For non-algorithmic subproblems the conjecture ticket (#6) is load-bearing; treating the ladder as *the* anti-fabrication story slightly mis-frames the harness as attack-centric. Prefer “every claim class has a named cheap ticket artifact.”  \n- No proposal here weakens §0/gates/taxonomy/baseline.\n\n### (c) Facts the plan doesn’t state (needed to implement, not to redesign)\n- Operator host: Linux cgroup + `landrun` available for M1 gold path, or macOS-only dev with accepted sandbox gap.  \n- Whether 60-bit was *intended* as post-admission (this review assumes yes given cost text).  \n- Write rate target for M4 parallel tracks (to size the single-writer fixture).  \n- Default budget slack (2×?) and whether wall-time, core-seconds, or both bind.  \n- Who may attribute skill yank clearances and salts (human-only vs orchestrator request + human grant, mirroring waivers).","ms":86538},{"model":"openrouter:z-ai/glm-5.2","ok":true,"content":"Let me carefully review this plan. It's a very detailed architecture document for \"Cairn,\" an autonomous math research harness focused on ECDLP. I need to find genuine improvements, not manufacture changes.\n\nLet me think about what's actually problematic or could be improved:\n\n1. **The ladder's A/A null arm** - The plan says the A/A null arm's CI radius sets the decision band. But there's a subtlety: the A/A arm compares the baseline against itself, which gives you the noise floor. The KEEP threshold is `1 + 2·radius`. This seems sound but I want to check if there's a multiple-comparisons issue across the multiple rungs (30/40/50/60 bits).\n\n2. **The foundations auditor sampling** - The spot-check bound `l ≥ ln(2^bits)/ln(1/(1−f))` is a standard coverage bound. But the plan says it's applied \"per tier stratum\" and within Tier-2/3 \"nodes with the largest justified_by fan-in are drawn first.\" This is sound but there's a question about what happens when a re-run fails - the plan says it downgrades and re-derives transitively, but doesn't specify whether the failed node's children are re-queued for re-verification or just downgraded.\n\n3. **The terminal-status invariant** - Every opened branch/claim must reach terminal status. But what about claims that are justified by evidence that later gets downgraded? The claim's tag gets re-derived, but does the claim itself need to reach a new terminal status? The plan says withdrawal is terminal, but a downgraded claim isn't withdrawn - it's just re-tagged. Is there a gap here?\n\n4. **The gate bundle** - It's a separate SQLite file opened read-only. But the plan says the orchestrator process opens it read-only. What about the gate process? Is the gate a separate process? The plan says gates are \"mechanical, immutable\" but doesn't clearly specify the process boundary. If the gate runs in the same process as the orchestrator, the read-only SQLite file is the only boundary, which is weak (the process can open it read-write if it wants). This seems like a real gap.\n\n5. **The recipe key canonicalizer** - The plan says \"Two logically equal recipes that hash differently are a canonicalizer bug, never a feature.\" But the canonicalizer is itself code that could have bugs. The plan has a known-answer test vector for it, but there's no mechanism for handling a canonicalizer bug discovered after results have been stored. If the canonicalizer changes, all old keys are stale. The `salt` field handles tool-version faults, but canonicalizer bugs are different - they affect the key construction itself.\n\n6. **The disagreement protocol** - Disagreements are \"never resolved by vote\" and are \"recorded with both artifacts' hashes, classified, routed to an owner.\" But what happens when the owner is the human and no human session is open? The plan says \"the claim's tag stays at its pre-dispute level until resolved.\" This is sound. But what about a disagreement between two workers on whether a branch should be funded? That's not a claim-level disagreement. Actually, the plan scopes this to \"Prover/Skeptic or panel disagreement\" so it's about claims and problem selection.\n\n7. **The cost profile** - Skills declare cost profiles, and \"a persistent mismatch is a strategy-layer alarm, never a gate.\" But what if a skill consistently underestimates its cost? The tier gate uses the declared cost to decide admission. If the declared cost is too low, work gets admitted to a higher tier than it should. The alarm fires after the fact, but the gate already let it through. This seems like a real gap - the cost profile is load-bearing for the tier gate but only checked after the fact.\n\n8. **The 60-bit rung** - The plan says a full 10²-trial distribution check at 60 bits is Tier-2 work. But the 60-bit rung is supposed to be the \"completion-and-extrapolation rung\" that tests whether the model fitted on ≤50 bits predicts the 60-bit mean. If the 60-bit rung requires Tier-2 budget, and Tier-2 requires a ladder result table with verdict KEEP as a ticket, there's a circular dependency: you need the 60-bit rung to get KEEP, but you need KEEP to get Tier-2 budget for the 60-bit rung. Let me re-read...\n\nActually, re-reading §6: \"the 60-bit rung is the completion-and-extrapolation rung\" and \"a full 10²-trial distribution check at 60 bits is Tier-2 work by its own declared profile, not Tier-1, and runs only when the tier gate grants that budget.\" And §5 says \"for Tier 2 on an algorithmic claim the ticket is a ladder result table with verdict KEEP (§6).\"\n\nSo the question is: can you get a KEEP verdict from the ≤50-bit rungs alone, and then use that KEEP to get Tier-2 budget for the 60-bit rung? Let me re-read §6 step (2b): \"The 60-bit rung is the completion-and-extrapolation rung: the model fitted on the ≤ 50-bit rungs must predict the measured 60-bit mean inside the band, and the method must complete and recover x on at least 10 fresh instances.\"\n\nAnd step (5): \"The result table... is the ladder's artifact, the Tier-2 ticket (§5) when its verdict is KEEP.\"\n\nSo the KEEP verdict requires the 60-bit rung to pass, but the 60-bit rung needs Tier-2 budget, which requires a KEEP verdict. This is a circular dependency. Unless the ≤50-bit rungs can produce a provisional KEEP that grants Tier-2 access for the 60-bit rung specifically. But the plan doesn't say this.\n\nActually, wait. Let me re-read more carefully. The plan says \"at least 10 fresh instances\" at 60 bits, not 10². And \"a full 10²-trial distribution check at 60 bits is Tier-2 work.\" So maybe the 10-instance completion check is Tier-1, and only the full 10² distribution check is Tier-2? But 10 instances at 60 bits is still ~1.3×10^10 group ops total, which is minutes in compiled code. That could be Tier-1 (minutes). But the plan says \"a full 10²-trial distribution check at 60 bits is Tier-2 work by its own declared profile, not Tier-1.\" It doesn't explicitly say the 10-instance check is Tier-1.\n\nThis is a real ambiguity/circular dependency. Let me flag it.\n\n9. **The `skip_cache_lookup` bit** - The Skeptic's re-runs always set it. But what about the foundations auditor's re-runs? The plan says \"The re-run it performs is the §3 policy\" but doesn't explicitly say it sets `skip_cache_lookup`. If it doesn't, the auditor might just re-read the cached value and \"verify\" it's the same, which is meaningless. Actually, re-reading §3: \"skip_cache_lookup (run even if cached and record a fresh attempt — never overwrite; the Skeptic's re-runs always set it).\" The foundations auditor should also set it. This seems like a gap.\n\n10. **The claim statement node and the Challenge** - The claim statement node has \"formal statement (the Challenge source, when one exists)\". The Challenge is compiled by the gate from the claim statement node. But who writes the formal statement in the claim statement node? The Reframer, Prover, or human. And the gate renders the Challenge from it. But what if the formal statement in the node is ambiguous or incomplete? The gate renders a Challenge from it, but the rendering could be wrong. The plan says \"the Formalizer never creates or edits Challenge text\" - so the gate renders it. But what does \"renders\" mean? Is there a template? This seems underspecified.\n\nActually, I think the claim statement node contains the Lean source for the Challenge, and the gate compiles it. The \"renderer\" might just be extracting the source from the node and compiling it. Let me not over-think this.\n\n11. **The `justify` function** - It \"checks that the evidence's declared target, assumptions and population cover that exact statement and returns a typed justification or a lattice violation, never a boolean.\" This is a critical function. How is it implemented? It needs to compare the evidence's target with the claim statement. For formal evidence (Lean proofs), this is straightforward (same statement hash). For empirical evidence (ladder results), it needs to check that the ladder was run on the right family with the right parameters. This seems like it could be gamed if the comparison is too loose. But the plan says it returns a \"typed justification or a lattice violation\" which suggests it's structural, not semantic. This seems sound but complex.\n\n12. **The near-duplicate advisory** - \"a 64-bit SimHash over the hypothesis text; no embedding model before the Librarian's retrieval stack exists.\" SimHash on hypothesis text. But the hypothesis key is over a typed hypothesis object, not text. So the SimHash is on the text representation, not the structural representation. This means two structurally identical hypotheses with different text would have different SimHashes but the same hypothesis key. The plan says \"paraphrase is the near-duplicate advisory's job\" - so this is intentional. But SimHash on text is a weak signal for mathematical near-duplicates, where the same idea can be expressed in very different words. This is a known limitation and the plan acknowledges it's \"advisory only.\" I think this is sound as stated.\n\n13. **The `author_supplied` corpus cap** - \"a corpus supplied only by the worker that authored the skill is not a self-test, and author_supplied alone caps the skill's results at CONJECTURE.\" But what about a skill authored by a human? The human is the operator. If the human authors a skill with only their own corpus, it's capped at CONJECTURE. But the human might have good reasons for the corpus. The plan says \"STRONG-EMPIRICAL needs an upstream corpus, an independent implementation under the axis rule below, or a postcondition or metamorphic relation.\" This seems right - even human-authored skills need independent validation.\n\n14. **The gate bundle hash pin** - \"whose hash is pinned at deployment and recorded in every gate-run record; a gate whose bundle hash differs from the pin fails closed.\" But who pins it? The deployment process. And who controls the deployment process? The operator. This is outside the orchestrator's write path, which is correct. But what about updates to the gate bundle? When the ladder plan changes (e.g., new sizes), the gate bundle changes, and the pin must be updated. The plan says \"a waiver is explicit, attributed, reason-bearing and expiring, issued only by a human\" - but updating the gate bundle isn't a waiver, it's a deployment. The plan doesn't specify the update protocol for the gate bundle. This seems like a gap.\n\nActually, re-reading: \"the gate bundle: a content-addressed bundle held in a separate store the orchestrator process opens read-only (a separate SQLite file at M0), whose hash is pinned at deployment and recorded in every gate-run record.\" So each gate run records which bundle hash it used. If the bundle is updated, new runs use the new hash, and old runs keep their old hash. This is sound - it's content-addressed. But the \"pin\" is what the current deployment expects. If someone updates the bundle without updating the pin, gates fail closed. If someone updates the pin without updating the bundle, gates fail closed. This seems correct.\n\n15. **The `Replayable` at Tier 2/3 policy** - \"re-run at the moment a promotion past CONJECTURE is sought (the promotion waits on it), otherwise sampled by the foundations auditor.\" But what if a Tier-2 result is used as evidence for a STRONG-EMPIRICAL claim, and then the foundations auditor's sample doesn't include it? The claim is tagged STRONG-EMPIRICAL without a re-run. The plan says \"the claim's tag capped at CONJECTURE meanwhile\" - wait, let me re-read: \"Replayable at Tier 2/3 ⇒ re-run at the moment a promotion past CONJECTURE is sought (the promotion waits on it), otherwise sampled by the foundations auditor (§7) with the claim's tag capped at CONJECTURE meanwhile.\"\n\nSo the claim is capped at CONJECTURE until the re-run happens. The re-run happens either at promotion time or when the auditor samples it. This is sound. But \"promotion past CONJECTURE\" - who initiates the promotion? The orchestrator? A worker? The plan doesn't say. If the orchestrator initiates it, the orchestrator could choose not to, leaving the claim at CONJECTURE forever. But that's fine - CONJECTURE is honest. If a worker initiates it, the worker would need to request the re-run. This seems like it needs clarification.\n\n16. **The `do_not_cache` bit** - When is this set? The plan says \"never memoize.\" But for what kinds of results? AuditOnly results? Failed results? The plan says \"status ≠ OK ⇒ never cached\" separately. So `do_not_cache` is for OK results that should never be memoized. When would you want an OK result to not be cached? Maybe for results that are too context-dependent? The plan doesn't give an example. This seems underspecified but not critical.\n\nLet me now focus on the most impactful changes:\n\n**A. The 60-bit rung circular dependency** - This is a real structural issue. The KEEP verdict requires the 60-bit rung, but the 60-bit rung needs Tier-2 budget, which requires KEEP. I need to propose a fix.\n\n**B. The gate process boundary** - The plan says the gate bundle is in a separate SQLite file opened read-only by the orchestrator process. But if the gate runs in the orchestrator process, the process can open the file read-write. The plan needs to specify that the gate runs in a separate process, or that the file permissions enforce read-only at the OS level.\n\n**C. The foundations auditor should set `skip_cache_lookup`** - The plan explicitly says the Skeptic does, but doesn't say the auditor does. This is a gap.\n\n**D. The cost profile as a tier gate input** - The declared cost profile is load-bearing for the tier gate but only checked after the fact. A skill that consistently underestimates its cost gets work admitted to higher tiers. The plan needs a mechanism to escalate a persistent underestimation to a gate-level constraint (e.g., temporarily capping the skill's declared tier).\n\n**E. The `justify` function's implementation** - This is the most critical function in the system (it derives all tags) but its implementation is underspecified. How does it compare an evidence node's target with a claim statement? For formal evidence, it's a hash comparison. For empirical evidence, what is it? The plan says it \"checks that the evidence's declared target, assumptions and population cover that exact statement\" but doesn't say how. This is a M0-level concern since M0 includes \"the §7 derived-tag rule (justify).\"\n\nLet me think about which of these are the most impactful and write them up.\n\nActually, let me reconsider the 60-bit circular dependency more carefully.\n\n§6 step (2b): \"The 60-bit rung is the completion-and-extrapolation rung: the model fitted on the ≤ 50-bit rungs must predict the measured 60-bit mean inside the band, and the method must complete and recover x on at least 10 fresh instances; the fit-on / hold-out split is fixed in the ladder plan (gate bundle, §4), never chosen by the worker, and a clean in-sample fit that misses out of sample is REJECT.\"\n\n§6 step (5): \"The result table (size, trials, mean and sd of ops, memory, success rate, fit to the pre-registered model, out-of-sample prediction and its band) is the ladder's artifact, the Tier-2 ticket (§5) when its verdict is KEEP.\"\n\n§5: \"for Tier 2 on an algorithmic claim the ticket is a ladder result table with verdict KEEP (§6)\"\n\n§6 step (2b) also says: \"a full 10²-trial distribution check at 60 bits is Tier-2 work by its own declared profile, not Tier-1, and runs only when the tier gate grants that budget.\"\n\nSo the 60-bit rung has two parts:\n1. 10-instance completion check (cost unclear)\n2. 10²-trial distribution check (Tier-2)\n\nAnd the KEEP verdict requires the 60-bit rung to pass. But the 60-bit rung's distribution check is Tier-2, which requires KEEP.\n\nI think the intended reading is:\n- The ≤50-bit rungs produce a provisional verdict\n- The 60-bit rung's 10-instance completion check is Tier-1 (minutes)\n- The 60-bit rung's full distribution check is Tier-2\n- The KEEP verdict requires the full 60-bit distribution check\n\nBut this creates the circular dependency. Unless:\n- The ≤50-bit rungs alone can produce a provisional KEEP that grants Tier-2 access specifically for the 60-bit distribution check\n- The 60-bit distribution check then confirms or rejects the KEEP\n\nThis would be a two-phase gate: provisional KEEP from ≤50 bits, then full KEEP from 60 bits. But the plan doesn't say this.\n\nAlternatively, maybe the 10-instance completion check at 60 bits is sufficient for KEEP, and the full 10² distribution check is for something else (like STRONG-EMPIRICAL vs CONJECTURE). But the plan says \"the ≤ 50-bit figures are STRONG-EMPIRICAL; the 60-bit figures carry the CONJECTURE tag their source assigns them.\" So the 60-bit figures are CONJECTURE anyway. And the KEEP verdict is about whether the method is an advance, not about the tag.\n\nI think the cleanest fix is: the ≤50-bit rungs produce a provisional KEEP that grants Tier-2 access for the 60-bit rung specifically. The 60-bit rung then confirms or rejects. The final KEEP verdict includes the 60-bit result. This breaks the circularity.\n\nLet me also think about the cost profile issue more carefully.\n\n§5: \"every skill declares its cost profile (complexity in inputs, expected tier) as part of its typed interface, so the orchestrator predicts spend before launch and the tier gate enforces cheap-before-expensive automatically.\"\n\n§5: \"Measured cost is compared against the declared profile; a persistent mismatch is a strategy-layer alarm (§15 P4), never a gate.\"\n\nSo the cost profile is used by the tier gate to decide admission, but a mismatch is only a strategy-layer alarm. This means a skill that declares \"Tier-1, minutes\" but actually takes hours would get work admitted to Tier-1, and the alarm would fire after the fact. The work already ran at the wrong tier. This is a real gap.\n\nThe fix could be: a persistent positive mismatch (actual cost > declared cost) escalates the skill's effective declared tier. After N mismatches, the skill's declared cost profile is automatically adjusted upward (to match observed cost), and this adjustment is recorded and attributed. This would be a gate-level mechanism, not just a strategy alarm. But the plan says \"a persistent mismatch is a strategy-layer alarm, never a gate.\" So this would be a change to the plan's design.\n\nActually, I think the plan's position is defensible: the cost profile is a prediction, and predictions can be wrong. The alarm catches persistent errors. The question is whether the alarm has teeth. If the alarm just logs, it has no teeth. If the alarm adjusts the cost profile, it has teeth but it's a gate. The plan says it's \"never a gate\" - so it's a strategy-layer alarm. But a strategy-layer alarm that the orchestrator can ignore is useless. The plan should specify that the alarm has a consumer: the orchestrator's allocation mechanism, which should down-weight skills with persistent cost overruns. But that's already implied by \"info-per-dollar\" allocation.\n\nActually, I think the real issue is simpler: a skill that underestimates its cost could get Tier-2 work admitted as Tier-1, bypassing the Tier-2 ticket requirement. The tier gate checks the declared tier, not the actual runtime. If the skill says \"Tier-1\" but actually runs for hours, the tier gate lets it through as Tier-1 without a ticket. The fix is: the tier gate should also check the actual runtime against the tier boundary, and if the actual runtime exceeds the declared tier's budget, the result is inadmissible (not just alarming). This would be a gate-level check.\n\nBut this is tricky: you can't know the actual runtime before you run. And after you run, the result is already computed. The plan's approach (alarm after the fact) is reasonable, but it should have teeth: after a persistent mismatch, the skill's declared cost profile is adjusted, and old results are not retroactively inadmissible (they were admitted under the old profile), but new launches use the adjusted profile. This is a strategy-layer adjustment with gate-level consequences for future launches.\n\nHmm, but the plan says \"a persistent mismatch is a strategy-layer alarm (§15 P4), never a gate.\" The \"never a gate\" is the issue. I think the fix is: the alarm triggers a cost-profile re-declaration, and the re-declaration is a gate-level event (the skill's interface version changes, so old cached results keep their old version and new launches use the new profile). This is consistent with the plan's versioning mechanism.\n\nActually, I think I'm overcomplicating this. Let me focus on the clearest, most impactful changes.\n\nLet me also think about the `justify` function. The plan says M0 includes \"the §7 derived-tag rule (justify) with append-only tag history.\" So `justify` must be implemented at M0. But the plan doesn't specify how it works for empirical evidence. For formal evidence, it's a statement hash comparison. For ladder evidence, it needs to check that the ladder was run on the right family. How?\n\nThe claim statement node has \"scope and model assumptions, quantities and units.\" The ladder result table has \"size, trials, mean and sd of ops, memory, success rate, fit to the pre-registered model, out-of-sample prediction and its band.\" The `justify` function needs to check that the ladder's family matches the claim's scope. But \"family\" is a mathematical concept, not a hash. How does `justify` compare them?\n\nI think the answer is: the claim statement node's \"scope and model assumptions\" field is a typed object (not free text), and the ladder's instance-maker parameters are also typed, and `justify` compares them structurally. But the plan doesn't specify the type of the scope field. This is a gap that would block M0 implementation.\n\nOK, let me now write up my proposals. I'll focus on the most impactful ones.\n\n1. **60-bit rung circular dependency** (HIGH) - The KEEP verdict requires the 60-bit rung, but the 60-bit rung's distribution check is Tier-2, which requires KEEP. Fix: ≤50-bit rungs produce a provisional KEEP that grants Tier-2 access for the 60-bit rung specifically.\n\n2. **Gate process boundary** (HIGH) - The gate bundle is a separate SQLite file opened read-only, but if the gate runs in the orchestrator process, the process can open it read-write. Fix: specify that the gate runs in a separate process, or that the file is opened with OS-level read-only permissions.\n\n3. **Foundations auditor must set `skip_cache_lookup`** (MED) - The plan explicitly says the Skeptic does, but doesn't say the auditor does. Without it, the auditor's re-runs could hit the cache and \"verify\" nothing.\n\n4. **Cost profile mismatch needs teeth** (MED) - A persistent cost underestimate gets work admitted to the wrong tier. The alarm fires after the fact but has no gate-level consequence. Fix: after a persistent mismatch, the skill's cost profile is re-declared (new interface version), and future launches use the adjusted profile.\n\n5. **`justify` function's comparison mechanism for empirical evidence** (MED) - The plan says `justify` checks that evidence covers the exact statement, but doesn't specify how for empirical evidence. Fix: specify that the claim's scope field and the evidence's target field are typed objects compared structurally.\n\n6. **The terminal-status invariant and downgraded claims** (LOW) - A downgraded claim isn't withdrawn (terminal), but its tag changed. Does it need a new terminal status? The plan says \"every opened claim is a linear obligation that must reach a terminal status.\" A downgraded claim has reached a terminal status (e.g., promoted to STRONG-EMPIRICAL, then downgraded to CONJECTURE). The downgrade is a tag transition, not a status transition. The claim is still \"promoted\" in status but CONJECTURE in tag. This seems fine - the status is about the branch lifecycle, not the tag. But it could be confusing. Actually, I think this is fine as stated. Let me not propose this.\n\nLet me also think about whether there are any missing gates or failure modes.\n\nOne thing I notice: the plan says \"a worker that exits leaving one open produces a Leaked record.\" But what if a worker crashes? The harness should detect the crash and produce the Leaked record. The plan says \"fail-fast in tests, logged and escalated in production.\" This seems sound.\n\nAnother thing: the plan says \"the orchestrator may rewrite the branch tree, funding, worker prompts, and subproblems freely.\" But if the orchestrator can rewrite worker prompts, it could rewrite a worker's prompt to bypass a gate. The plan says \"in whose context no gate rubric or detection logic is reachable\" - but the orchestrator composes the prompt. If the orchestrator puts gate rubric in the prompt, the worker could use it to game the gate. The plan says the worker's prompt is \"exactly the nodes it is handed\" and \"no gate rubric or detection logic is reachable.\" But the orchestrator is the one composing the prompt. If the orchestrator is compromised (by a worker that gained too much influence), it could inject gate rubric. The plan's defense is that the gate is mechanical and immutable, so even if the worker knows the gate rubric, it can't bypass the gate. This is sound - the gate doesn't depend on the worker not knowing it. The plan says \"context isolation is a property of the dispatch, not of the model\" and \"a worker with a general file read can open what its context hides.\" But the gate runs in a separate process (or at least from a separate store), so the worker can't modify the gate even if it knows the rubric. This is sound.\n\nActually, wait - I said the gate process boundary is unclear. Let me re-read: \"the gate bundle: a content-addressed bundle held in a separate store the orchestrator process opens read-only (a separate SQLite file at M0), whose hash is pinned at deployment and recorded in every gate-run record; a gate whose bundle hash differs from the pin fails closed.\"\n\nSo the orchestrator process opens the gate bundle read-only. But the gate logic itself - where does it run? In the orchestrator process? If so, the orchestrator could skip the gate logic even if it can't modify the bundle. The plan says \"every gate ships a planted-failure self-test\" and \"gates run from a declarative plan with expected exit codes and ordered scopes.\" But if the gate logic is in the orchestrator process, the orchestrator could skip it. The plan's defense is the hash-chained log: \"an orchestrator that rewrites history produces a log that fails verification.\" But if the orchestrator skips a gate, it just doesn't write a gate-run record. The log doesn't have a record to verify. The plan needs a mechanism to detect missing gate runs.\n\nActually, the plan says \"a result not in the substrate does not exist to the claims DB.\" And \"a cached value is served only from a verified attempt.\" So if the orchestrator skips the gate, the result doesn't have a gate-run record, and it's not admissible. But who checks this? The claims DB? The `justify` function? If `justify` checks for a gate-run record, then a result without one is not justifiable. This seems sound - the gate is enforced by the claims DB, not by the gate process.\n\nBut wait - the claims DB is also in the orchestrator process (or is it?). The plan says \"the orchestrator process opens read-only\" the gate bundle. But the claims DB is part of the state layer, which the orchestrator can write. So the orchestrator could write a claim without a gate-run record. The `justify` function would need to check for the gate-run record. But `justify` is part of the gate layer, which is immutable. If `justify` runs in the orchestrator process, the orchestrator could skip it.\n\nI think the key question is: is there a separate process for the gate layer? The plan doesn't explicitly say so. It says the gate bundle is in a separate store, but it doesn't say the gate logic runs in a separate process. This is a real gap.\n\nThe fix: the gate layer runs in a separate process (a \"gate daemon\") that the orchestrator can only invoke, not modify. The gate daemon reads the gate bundle, runs the gate logic, and writes gate-run records to the substrate. The orchestrator can request a gate run but cannot skip it or modify its result. The claims DB's `justify` function is part of the gate daemon, not the orchestrator.\n\nThis is a significant architectural change, but it's necessary for the \"immutable epistemics\" principle to hold mechanically. Without it, the gate is a convention, not a mechanism.\n\nActually, let me reconsider. The plan says \"the gate layer (mechanical, immutable)\" and lists it as a component. It says \"outside the orchestrator's write path.\" But it doesn't specify the process boundary. The separate SQLite file is a store boundary, not a process boundary. If the orchestrator process can open the file read-write (by just opening a new connection), the store boundary is a convention, not a mechanism.\n\nThe fix could be simpler: the gate bundle file has OS-level read-only permissions (chmod 444), and the orchestrator process runs as a user that cannot modify it. This is a deployment-level constraint, not a code-level one. The plan should specify this.\n\nOr: the gate runs in a separate process, and the orchestrator communicates with it via a pipe or socket. The gate process has its own credentials and the gate bundle is only accessible to the gate process.\n\nI think the simplest fix is to specify that the gate bundle file is owned by a separate user/account and the orchestrator process runs with reduced privileges that cannot write to it. This is a deployment constraint that should be specified in the plan.\n\nLet me also think about one more thing: the plan says \"the orchestrator may rewrite the branch tree, funding, worker prompts, and subproblems freely.\" But the branch tree is part of the state layer, which includes the dead-end ledger. The plan says \"the orchestrator may rewrite the tree but never REFUTED entries.\" The REFUTED entries are in the ledger, which is an \"append-only, hash-chained log.\" So the orchestrator can't rewrite REFUTED entries because the log is tamper-evident. But the orchestrator can append new entries. Could the orchestrator append a new PARKED entry that supersedes a REFUTED entry? The plan says \"REFUTED (proven dead → permanent blacklist)\" and \"PARKED (blocked-not-dead).\" These are different statuses. A PARKED entry can't supersede a REFUTED entry because they have different hypothesis keys (or do they?). Actually, the same hypothesis could be both REFUTED and PARKED if different evidence supports each. But the preflight checks REFUTED first: \"Blocked = the hypothesis key matches a REFUTED entry whose retry predicate is unmet.\" So a REFUTED entry blocks new branches regardless of PARKED entries. This seems sound.\n\nOK, I think I have enough. Let me write up my proposals.\n\nLet me also think about the framing question. The plan's framing is \"autonomous math research harness.\" Is this the right framing? I think the framing is sound - the plan is clear about what it is and what it isn't. The \"mutable strategy, immutable epistemics\" principle is the right core. The honest baseline is load-bearing. I don't think the framing is wrong.\n\nOne thing I might say about framing: the plan frames itself as a system for \"open-ended research on hard problems\" with ECDLP as the motivating instance. But the architecture is very specifically tuned to ECDLP - the ladder, the no-go checklist, the verifier. This is fine for a motivating instance, but the plan should acknowledge that the architecture would need different gates for different problems. The plan does say \"the motivating instance\" and \"the realistic target is publishable increments on open subproblems,\" so it's not claiming generality. I think this is fine.\n\nActually, let me reconsider one more time. The plan says the gate layer is immutable and includes \"submission verifier (xP==Q subprocess).\" This is ECDLP-specific. If the system were to work on a different problem, the verifier would need to change. But the plan says the gate layer is immutable - so changing the verifier would require a new deployment. This is fine - the plan is for a specific instance. But the plan should be clear that the gate layer is immutable within a deployment, not across deployments. I think this is implied but not stated.\n\nOK, let me finalize my proposals. I'll aim for 5-6 high-quality ones.\n\n1. **60-bit rung circular dependency** (HIGH, §6)\n2. **Gate process boundary** (HIGH, §4)\n3. **Foundations auditor must set `skip_cache_lookup`** (MED, §7)\n4. **Cost profile mismatch escalation** (MED, §5)\n5. **`justify` comparison for empirical evidence** (MED, §7)\n6. Maybe one more...\n\nActually, let me think about one more: the plan says \"the orchestrator may request a [waiver], never grant one.\" But the orchestrator could request a waiver for every gate, flooding the human queue. The plan says \"a waived check never counts as satisfied\" and \"the admission gates are non-waivable.\" So waivers are only for operational checks, not admission gates. And \"a waiver may only let an explicitly operational check continue as AuditOnly output.\" So waivers are limited. But the orchestrator could still flood the human queue with waiver requests for operational checks. The plan doesn't address this. But this is a minor operational issue, not a structural one. Let me not propose this.\n\nLet me think about one more potential issue: the plan says \"every skill ships a known-answer self-test\" and \"every skill ships a known-answer self-test\" with a \"committed pass floor.\" But what if a skill's self-test passes but the skill produces wrong results on inputs outside the corpus? The plan addresses this with the axis rule (cross-checks) and the randomized postcondition. But the randomized postcondition is only for Tier-0 arithmetic skills. What about Tier-1 skills? The plan says \"Tier-0 arithmetic skills verify their own answer before returning (the randomized postcondition; Sage's discrete_log checks base^res == a before it returns).\" But Tier-1 skills (like rho) don't have a simple postcondition. The plan says they produce a certificate (the witness), which is checked by the verifier. So the certificate is the postcondition for Tier-1 skills. This seems sound.\n\nOK, I think I have my proposals. Let me write them up concisely.\n\nActually, one more thought on the 60-bit circular dependency. Let me re-read the plan one more time...\n\n§6: \"a full 10²-trial distribution check at 60 bits is Tier-2 work by its own declared profile, not Tier-1, and runs only when the tier gate grants that budget.\"\n\n§5: \"for Tier 2 on an algorithmic claim the ticket is a ladder result table with verdict KEEP (§6)\"\n\nSo the ticket for Tier-2 is a ladder result table with verdict KEEP. The verdict KEEP requires the 60-bit rung. The 60-bit rung's distribution check is Tier-2. So you need KEEP to get Tier-2, and you need Tier-2 to get the 60-bit rung, and you need the 60-bit rung to get KEEP. Circular.\n\nBut wait - maybe the 60-bit rung's 10-instance completion check (not the full 10² distribution check) is Tier-1? The plan says \"the method must complete and recover x on at least 10 fresh instances\" at 60 bits. 10 instances at 60 bits is ~1.3×10^10 group ops, which is minutes in compiled code. That's Tier-1. And the full 10² distribution check is Tier-2. So maybe the KEEP verdict only requires the 10-instance completion check (Tier-1), and the full distribution check is for something else?\n\nBut the plan says \"the model fitted on the ≤ 50-bit rungs must predict the measured 60-bit mean inside the band.\" The \"measured 60-bit mean\" requires multiple trials, not just 10. With 10 instances, you have a mean but it's very noisy (sd ≈ 0.5×mean). The band is set by the A/A null arm. With only 10 instances, the CI is wide. Can you make a KEEP/REJECT decision with 10 instances?\n\nActually, the plan says \"the method must complete and recover x on at least 10 fresh instances\" - this is a completion check, not a distribution check. And \"the model fitted on the ≤ 50-bit rungs must predict the measured 60-bit mean inside the band\" - this requires a mean, which you can compute from 10 instances, but it's noisy. The plan doesn't say how many trials are needed for the 60-bit mean prediction. It says \"a full 10²-trial distribution check at 60 bits is Tier-2 work\" - so 10² trials is the full check. But 10 trials might be enough for a provisional check.\n\nI think the plan's intent is:\n- ≤50-bit rungs: 10² trials each, Tier-1, produce the model fit\n- 60-bit rung: 10 trials for completion (Tier-1), 10² trials for distribution (Tier-2)\n- KEEP verdict requires the 60-bit completion check (Tier-1) and the model prediction check\n- The full 60-bit distribution check (Tier-2) is for STRONG-EMPIRICAL confirmation\n\nBut the plan doesn't clearly separate these. The \"verdict KEEP\" is defined in step (2) as \"the whole claim CI clears 1 + 2·radius\" which requires the distribution check. And step (2b) says the 60-bit rung is part of the KEEP verdict. So KEEP requires the 60-bit distribution check, which is Tier-2, which requires KEEP. Circular.\n\nI think the fix is: introduce a \"provisional KEEP\" verdict from the ≤50-bit rungs that grants Tier-2 access specifically for the 60-bit distribution check. The final KEEP verdict requires the 60-bit distribution check. This breaks the circularity.\n\nLet me write this up.\n\nActually, I realize there might be an even simpler reading. Maybe the ≤50-bit rungs alone can produce a KEEP verdict, and the 60-bit rung is an additional check that can REJECT but not produce KEEP. Let me re-read...\n\n§6 step (2): \"at the 30/40/50-bit rungs at least 10² independent trials per size... the sorted-distribution shape compared to the claimed model... a speedup is KEPT only if the whole claim CI clears 1 + 2·radius.\"\n\n§6 step (2b): \"The 60-bit rung is the completion-and-extrapolation rung: the model fitted on the ≤ 50-bit rungs must predict the measured 60-bit mean inside the band, and the method must complete and recover x on at least 10 fresh instances... a clean in-sample fit that misses out of sample is REJECT.\"\n\nSo step (2) defines KEEP based on the ≤50-bit rungs. Step (2b) adds the 60-bit rung as an additional check that can REJECT. So KEEP is produced by ≤50-bit rungs, and the 60-bit rung can override it to REJECT. This would mean:\n- ≤50-bit rungs produce KEEP → Tier-2 ticket\n- 60-bit rung (Tier-2) can REJECT, overriding KEEP\n\nBut this creates a different problem: the 60-bit rung can REJECT a claim that already has a Tier-2 ticket. The Tier-2 work was already done (the 60-bit distribution check). If it REJECTs, the claim is refuted. But the Tier-2 ticket was used to get the Tier-2 budget for the 60-bit check. So the claim got Tier-2 budget, ran the 60-bit check, and was rejected. This is fine - it's the normal flow. The Tier-2 ticket from ≤50-bit rungs grants Tier-2 budget for the 60-bit check, which can confirm or reject.\n\nWait, but §5 says \"for Tier 2 on an algorithmic claim the ticket is a ladder result table with verdict KEEP (§6).\" If the ≤50-bit rungs produce KEEP, that's the ticket. The 60-bit rung is Tier-2 work that uses the ticket. If the 60-bit rung REJECTs, the claim is refuted. This seems to work!\n\nBut then what does \"the result table... is the Tier-2 ticket when its verdict is KEEP\" mean? The result table includes the 60-bit data. If the 60-bit rung hasn't run yet (it's Tier-2), the result table only has ≤50-bit data. Can that produce KEEP?\n\nI think the plan is ambiguous here. The result table is defined in step (5) as including \"out-of-sample prediction and its band\" which is the 60-bit data. So the result table requires the 60-bit rung. But the 60-bit rung is Tier-2. And the Tier-2 ticket is the result table with KEEP. Circular.\n\nOK, I'm now confident this is a real circular dependency. Let me propose the fix.\n\nThe fix: split the verdict into two phases:\n1. **Provisional KEEP** from ≤50-bit rungs (Tier-1): the model fits in-sample, the CI clears the band, BSGS is beaten. This is the Tier-2 ticket.\n2. **Confirmed KEEP** after the 60-bit rung (Tier-2): the model predicts out-of-sample, the method completes. This upgrades the ladder result from provisional to confirmed.\n\nA provisional KEEP that fails the 60-bit rung is REJECT. A provisional KEEP that passes is confirmed KEEP. The Tier-2 ticket is the provisional KEEP. The confirmed KEEP is the final verdict.\n\nThis is clean and breaks the circularity. Let me write it up.\n\nNow let me also think about the gate process boundary issue more carefully.\n\nThe plan says: \"the gate bundle: a content-addressed bundle held in a separate store the orchestrator process opens read-only (a separate SQLite file at M0), whose hash is pinned at deployment and recorded in every gate-run record; a gate whose bundle hash differs from the pin fails closed.\"\n\nThe key phrase is \"the orchestrator process opens read-only.\" This means the orchestrator process has a read-only file handle to the gate bundle. But the orchestrator process could also open a read-write file handle to the same file (unless OS permissions prevent it). The plan doesn't specify OS-level permissions.\n\nMore importantly, the gate logic itself - where does it run? If it runs in the orchestrator process, the orchestrator could skip it. The plan says \"gates run from a declarative plan with expected exit codes and ordered scopes, where an earlier failure blocks later ones.\" But if the orchestrator process is the one running the gates, it could choose not to run a gate. The hash-chained log would show a missing gate-run record, but only if someone checks. The `justify` function checks for gate-run records, but if `justify` also runs in the orchestrator process, it could be skipped too.\n\nThe fix: the gate layer runs in a separate process. The orchestrator can submit work to the gate process, but cannot modify the gate process or skip its checks. The gate process writes gate-run records to the substrate. The `justify` function runs in the gate process. The orchestrator can read gate-run records but cannot write them.\n\nThis is a significant architectural change, but it's necessary for the \"immutable epistemics\" principle to hold mechanically. The plan says \"outside the orchestrator's write path\" but doesn't specify how. A separate process is the simplest mechanism.\n\nActually, the plan says \"a separate SQLite file at M0.\" This is a store boundary. But it also says \"the orchestrator process opens read-only.\" If the orchestrator process opens the file read-only (O_RDONLY), it can't write to it through that handle. But it could open another handle. Unless the file permissions prevent it.\n\nI think the simplest fix is: the gate bundle file is owned by root (or a gate user), the orchestrator process runs as a non-root user, and the file has 0444 permissions. The orchestrator process literally cannot open the file for writing. This is a deployment constraint, not a code constraint. The plan should specify it.\n\nBut this doesn't address the gate logic running in the orchestrator process. Even if the orchestrator can't modify the gate bundle, it could skip the gate logic. The gate-run record would be missing, and `justify` would reject the claim. But `justify` runs in the orchestrator process too (it's part of the claims DB, which the orchestrator writes). So the orchestrator could skip `justify` too.\n\nThe fix: `justify` and the gate logic run in a separate process. The orchestr","ms":63295},{"model":"openrouter:deepseek/deepseek-v4-flash-0731","ok":true,"content":"We need answer user asks review plan, propose revisions. Need follow format. Need inspect plan. Need produce numbered proposals with title severity section rationale git-diff style. Need under 2500 words. Need end with three proposals stake most, framing wrong, facts needed. Need not manufacture changes. Need consider invariants/out of scope. Need maybe identify contradictions, missing gates, overengineering, sharpen definitions. Need be rigorous.\n\nWe need review entire plan. Need propose best revisions. Need maybe include \"sound sections need nothing\". Need not read repo. Need use evidence tags? For proposed mechanisms, adoption bar: name component, cost, evidence, confirm doesn't weaken invariants. Need include for each? User says adoption bar for any new mechanism. We can include in rationale. Need maybe not propose too many. Need focus.\n\nLet's understand plan deeply.\n\nCairn: autonomous math research harness. Mutable strategy, immutable epistemics. Thin agents fat skills. Content-addressed substrate. Components: state layer, orchestrator, workers, gate layer. Compute tiers. Ladder. Epistemic mechanics. No-go checklist. Never give up. Human loop. Compounding assets. research-software. Build order M0-M4. Honest outcomes. Deferred decisions. Evidence.\n\nNeed find issues.\n\nPotential issues:\n\n1. Hypothesis key includes \"claimed property or cost model (exponent, constant, crossover — or the claim statement hash of §4), method identity (skill@version and parameters, or the statement hash of the approach), declared parameter ranges, declared sampling distribution D where one exists).\" It excludes per-run seeds and ladder instances. But for measured refutations, \"A measured refutation reaches exactly the hypothesis object it was measured on, because that object is what was hashed; a broader blacklist needs a formal entry.\" However if hypothesis key includes method identity skill@version and parameters, then a refutation of a method at one parameter range only blocks same method+params. But \"same hypothesis, same key, already dead\" maybe okay. But there is a subtle issue: hypothesis key includes \"declared parameter ranges\" and \"declared sampling distribution D\". If a worker can declare broad parameter ranges, a refutation on small range blocks broad? Actually measured refutation reaches exactly object measured; if object has broad ranges, then refutation on subset? The key includes declared ranges, not actual measured ranges. If a hypothesis object declares range [30,60] and measured at 50, key matches any attempt with same declared range, even if actual measured range differs? The plan says \"A measured refutation reaches exactly the hypothesis object it was measured on, because that object is what was hashed.\" But if declared ranges are broad, a refutation measured on 50 bits blocks a future attempt that declares same broad range but plans to test 100 bits? The preflight sees same hypothesis key and blocks. Is that intended? It says \"declared parameter ranges\" in key, so yes. But maybe should include actual measured range in refutation evidence? The ledger entry has result + CI, method, sizes. The retry predicate can allow. But \"Blocked = hypothesis key matches REFUTED entry whose retry predicate is unmet.\" If same key but different actual range, blocked unless retry predicate. Could be okay because declared range is part of hypothesis. But maybe a worker can game by declaring narrow range to avoid collision? Then refutation only narrow. That's okay. Need maybe sharpen: hypothesis key should include \"declared parameter ranges\" but not \"actual measured range\"; refutation evidence includes actual. Fine.\n\n2. The \"claim statement node\" is written before proof/measurement, immutable. But \"supersedes\" edge. Evidence attached to superseded version does not transfer. Good. But \"claim identity\" includes \"version\" and \"source-claim hash\" maybe weird. Need maybe not.\n\n3. Gate bundle: \"held in a separate store the orchestrator process opens read-only (a separate SQLite file at M0), whose hash is pinned at deployment and recorded in every gate-run record; a gate whose bundle hash differs from the pin fails closed.\" But if orchestrator process opens read-only, can it be compromised? It can't write. But the orchestrator could perhaps use SQL injection? It's read-only. Good. However \"gate bundle\" includes \"every worker tool allow-list\" and \"ladder plan\" etc. If gate bundle is immutable and pinned, how are operator knobs changed? \"Whether Tier-2 spend also requires an open operator session is an operator knob in the gate bundle\" implies gate bundle can be updated by human. Need versioning. Maybe okay.\n\n4. \"The tag is derived, never set.\" \"A claim version's tag = strongest class some evidence node justifies via justify(evidence, statement hash)\" But \"PROVEN has non-waivable floor (artifact above must exist and re-verify).\" Good.\n\n5. \"Green Lean\" protocol: \"The gate compiles the Challenge module from the claim statement node with a gate-side renderer and pinned prelude, both inside the gate bundle and outside every worker's write path; the Formalizer never creates or edits Challenge text and submits a Solution module that names the Challenge's statement hash — a Solution naming any other hash fails closed before kernel replay.\" But \"statement hash is computed over what the comparator compares (the theorem's ConstantVal and the full ConstantInfo of every constant in its transitive closure), never over raw export bytes\". Need maybe issue: \"Challenge and Solution theorem statements to be identical over the statement's transitive constant closure\" but if Solution has extra definitions/imports, transitive closure may differ? It says \"redefined constant anywhere under the statement fails\". Need maybe okay.\n\n6. \"No-go checklist\" only three items. Maybe missing \"semaev summation polynomial / decomposition\" already in item 3. Good.\n\n7. \"Small-scale ladder\" protocol: \"at the 30/40/50-bit rungs at least 10² independent trials per size\" and \"60-bit rung ... at least 10 fresh instances\". But \"A 60-bit generic trial is ≈ 1.3×10⁹ group ops (minutes in compiled code, a quarter hour interpreted), so a full 10²-trial distribution check at 60 bits is Tier-2 work by its own declared profile, not Tier-1, and runs only when the tier gate grants that budget.\" Wait 10 fresh instances at 60 bits = 1.3e10 ops, maybe hours. Fine.\n\nPotential issue: Ladder acceptance bar \"KEEP only if whole claim CI clears 1 + 2·radius (floor 1%) on group operations and memory stays inside same band; otherwise INCONCLUSIVE or REJECT.\" But \"Refutation floor: at 50 bits BSGS ≈ 1.5√n ops with ≈ 3×10⁷ table entries; failing to beat it (~5×10⁷ group ops, under 1 GB) refutes on the spot\" Wait BSGS at 50 bits: sqrt(2^50)=2^25=33.5M, 1.5√n=50M ops, table entries 33.5M. \"failing to beat it (~5×10⁷ group ops, under 1 GB)\" okay. \"Acceptance bar: KEEP band above, against rho (≈ 1.25√n ≈ 4×10⁷ ops at 50 bits).\" Wait 1.25*sqrt(2^50)=1.25*33.5M=41.9M. Good.\n\nPotential issue: \"The band is always a ratio against the best generic baseline the harness ships, measured in the same harness on the same instances — plain rho (≈ 1.25√n ops, O(1) memory), never BSGS.\" But \"Refutation floor\" uses BSGS. Good.\n\nPotential issue: \"A/A null arm — the baseline run against itself in the same harness, same trial count, same instance stream — whose CI radius sets the decision band.\" Need maybe define \"CI radius\" and \"whole claim CI clears 1 + 2·radius\". Could be okay.\n\nPotential issue: \"The 60-bit rung is the completion-and-extrapolation rung: the model fitted on the ≤ 50-bit rungs must predict the measured 60-bit mean inside the band, and the method must complete and recover x on at least 10 fresh instances.\" But if method is randomized, \"fresh instances\" from gate-owned instance-maker. Good.\n\nPotential issue: \"The result table ... is the ladder's artifact, the Tier-2 ticket (§5) when its verdict is KEEP\" But \"Tier 2 on an algorithmic claim the ticket is a ladder result table with verdict KEEP\" Good.\n\nPotential issue: \"Tier gate predicate. The gate reads three things and nothing else: the skill's declared cost profile, the spawning budget (the meet above), and the branch's ticket — the most recent admissible substrate node (status OK, replay grade ≥ Verifiable, reproducibility-checked under the §3 policy) produced under the same branch at the tier below.\" \"most recent\" could be gamed? If a branch has a ticket from earlier, then later work at same tier? The ticket is \"most recent admissible substrate node ... produced under the same branch at the tier below.\" For Tier 2 on algorithmic claim, ticket is ladder result table with KEEP. If branch later produces a Tier-1 result that is not a ladder KEEP, \"most recent\" might be that, and gate refuses? Actually \"most recent admissible substrate node ... at the tier below\" could be any node, not specifically the required ticket. It says \"for Tier 2 on an algorithmic claim the ticket is a ladder result table with verdict KEEP\" so the ticket is a specific kind. But \"most recent admissible substrate node\" might be a different node. Need maybe clarify \"the branch's ticket is the most recent admissible substrate node of the required kind\" or \"the most recent admissible substrate node that satisfies the ticket predicate\". This is a potential ambiguity. Could be a revision.\n\nPotential issue: \"A spawned worker's budget is the meet of its parent's remaining budget and the skill's declared profile; a retry or restart is launched only if the remaining budget can afford it.\" \"meet\" maybe min. Good.\n\nPotential issue: \"Allocation currency is expected information per compute-dollar, not raw yield — a cheap experiment that halves a conjecture's probability outranks an expensive one that nudges a bound; until M3 decides its mechanics (§15 P3) it is implemented as gate-outcome reward per measured cost, and no model probability, e-value, posterior or similarity term ever enters it.\" But \"expected information\" is a model probability? They say no model probability enters. \"expected information per compute-dollar\" is a theoretical objective, but implementation is gate-outcome reward per measured cost. Fine.\n\nPotential issue: \"Compute is a tracked, finite resource flowing through the DAG exactly like claims.\" Good.\n\nPotential issue: \"Measured cost is compared against the declared profile; a persistent mismatch is a strategy-layer alarm (§15 P4), never a gate.\" But \"tier gate\" uses declared cost profile, not measured. If a skill persistently under-declares cost, it can get Tier-2 spend before alarm. Maybe should be a gate? But plan says \"never a gate\" because cost mismatch is operational, not epistemic. However if a skill's measured cost is 100x declared, the tier gate's \"declared cost exceeds remaining budget\" is based on declared, so it could admit a Tier-3 job as Tier-2. The plan says \"A declared cost profile above ≈ 10³ core-hours is a Tier-3 request whatever the skill is called.\" But if declared profile is wrong, measured cost mismatch alarm only after. Could be a missing gate: before a skill is admitted to a higher tier, its cost profile should be validated on lower-tier runs? Actually \"Measured cost is compared against the declared profile; a persistent mismatch is a strategy-layer alarm, never a gate.\" But \"a persistent mismatch\" could be after many runs. Maybe should be a gate for tier admission: a skill whose measured cost on Tier-1 exceeds its declared profile by a factor cannot be used for Tier-2 until profile updated? But updating profile is strategy? Skill version changes, self-test? Hmm. Need think. The plan's \"tier gate reads declared cost profile\" is vulnerable to a skill author (worker? skill is fixed version) under-declaring. But skills are built by humans? \"Thin agents, fat skills\" skills have fixed version and self-test. Who authors skills? Probably human or worker? The plan says \"Every skill ships a known-answer self-test.\" It doesn't say who authors. If a worker can author a skill with a false cost profile, then tier gate can be gamed. But maybe skill authoring is human? Need maybe propose that cost profile is part of skill identity and must be validated by a measured-cost self-test? The self-test includes \"declared cost profile\" but not necessarily measured. \"Granularity rule: grain a skill at 'a capability with a stable interface, a self-test, and a declared cost profile'.\" The self-test is known-answer, not cost. Could add: every skill's self-test includes a cost-profile check on a small benchmark, and a skill whose measured cost exceeds declared profile by a factor is not admissible for higher tier until profile corrected. But \"measured cost mismatch is never a gate\" contradicts. Maybe we can propose a \"cost-profile validation\" as part of skill admission, not a gate on individual launches. Need be careful with invariant? It doesn't weaken. It improves tier gate. Need evidence? This is architecture, not mechanism? Adoption bar: component tier gate; cost small; evidence STRONG-EMPIRICAL? We can say standard engineering practice, but need tag. Maybe \"PROVEN-in-source\" if in plan? No. We can tag CONJECTURE? Need maybe not propose if uncertain. But this is a real gap.\n\nPotential issue: \"The gate bundle ... whose hash is pinned at deployment and recorded in every gate-run record; a gate whose bundle hash differs from the pin fails closed.\" But \"operator knob in the gate bundle\" means human can update bundle. Need versioning and pin update process. Maybe okay.\n\nPotential issue: \"Every worker is a fresh-context agent dispatched by the harness as its own top-level query — never composed by a parent model — whose prompt is exactly the nodes it is handed, whose tool allow-list and substrate handle are named in its dispatch record, and in whose context no gate rubric or detection logic is reachable.\" \"whose prompt is exactly the nodes it is handed\" but \"Research-Mode prompt = template\" maybe template plus nodes. Fine.\n\nPotential issue: \"Skeptic sees only the statement, never the prover's reasoning\" but \"Its re-runs of any experiment set skip_cache_lookup\" If it re-runs experiment, it needs method/recipe, which is prover's reasoning? It says \"sees only the statement\" but to re-run an experiment it needs the recipe. Maybe \"statement\" means claim statement, not proof. But re-running an experiment requires the method. Could be a contradiction. Let's examine: \"Independent skeptic: sees only the statement, never the prover's reasoning; rewarded solely for gaps/counterexamples. If it reads the proof it just agrees — independence is the whole value.\" Then \"Its re-runs of any experiment set skip_cache_lookup (§3).\" To re-run an experiment, it must know the experiment's recipe (skill@version, inputs, seed). That is not the proof, but it is the method. Is that \"prover's reasoning\"? Maybe not. But \"sees only the statement\" is too strong. It needs the evidence node's recipe to re-run. Maybe the plan means it sees the statement and the experiment's declared method? Need clarify. Could be a revision: \"sees only the statement node and the evidence node's recipe (not the prover's narrative or proof)\" or \"the skeptic's re-run is dispatched by the harness with the recipe, not by the skeptic reading the proof.\" This is a real ambiguity.\n\nPotential issue: \"The tag is derived, never set.\" \"A claim version's tag = strongest class some evidence node justifies via justify(evidence, statement hash)\" But \"PROVEN has a non-waivable floor (the artifact above must exist and re-verify).\" Good.\n\nPotential issue: \"Strong law of small numbers (hard gate): a small-case pattern is a conjecture until it survives larger cases + a deliberate counterexample hunt. Whether this gate also gains a pre-registered quantitative floor ... is decided at M1 with planted-false conjectures (§15 P1); the floor, if adopted, never promotes a tag and never enters the orchestrator's reward.\" Good.\n\nPotential issue: \"Standing foundations-auditor ... sampling cadence for re-verifying substrate nodes uses the spot-check bound l ≥ ln(2^bits)/ln(1/(1−f)) so that undetected rot at fraction f has probability below 2^−bits; f and bits are named in the gate bundle (M0: f = 0.01, bits = 20), the bound is applied per tier stratum so that cheap Tier-0/1 nodes cannot absorb the whole sample, and within the Tier-2/3 stratum the nodes with the largest justified_by fan-in are drawn first.\" Need check formula: l ≥ ln(2^bits)/ln(1/(1−f)). For f=0.01, bits=20, l ≥ 13.86/0.01005 ≈ 1379. That's per stratum? If applied per tier stratum, each stratum needs 1379 samples. Fine. But \"undetected rot at fraction f has probability below 2^−bits\" if sampling without replacement? Formula is for iid? If finite population, hypergeometric. But okay. Maybe not.\n\nPotential issue: \"No-go checklist (auto-flag tripwire): Any proposed attack on the target must state how it evades all three or be flagged almost-certainly-broken.\" \"The gate is mechanical in the presence dimension — a missing or empty declaration is an automatic flag — and the flag has a consumer: the proportional-scrutiny router (§7) routes a flagged attack to the top scrutiny class and the tier gate (§5) grants it no ticket above Tier 1 until the declaration exists and has been reviewed; the content of a declared evasion is reviewed by the Skeptic and the human, never by this gate alone.\" Good.\n\nPotential issue: \"Never give up — defined so it can't become fabrication\" Good.\n\nPotential issue: \"Problem queue: open subproblems scored tractability × payoff × novelty, ranked and injectable by a human.\" \"Mixture-of-models as filter, not oracle.\" Good.\n\nPotential issue: \"The selector is itself audited. Problem-selection is a pluggable, measured component: track whether greenlit problems panned out, score its calibration, and swap it ... if that beats the panel on record. Taste is held to the system's own standard. 'Panned out' is adjudicated by a gate, never by the selector; the measurement protocol (a calibration test martingale on greenlight probabilities, with the outcome-lag caveat) is decided at M4 (§15 P2); the swap itself is a human decision informed by that monitor, never an automatic trigger, because the selector is the taste component and the human is the taste anchor.\" Good.\n\nPotential issue: \"Compounding assets\" \"Formalized-lemma library (shared, mathlib-style): every PROVEN lemma lands here\" \"Intake runs the §7 statement pre-filters and a symbolic-equivalence dedup (a positive signal only).\" Good.\n\nPotential issue: \"Where research-software fits\" \"It designs none of the above. Its one job: current, code-level truth on the stack before you wire it — Lean/mathlib, Sage/PARI/Magma, the orchestration framework, Claude Code's subagent/Task interface. Output: commands/config/gotchas per tool from the latest stable tag. De-risks the stack; touches no math.\" Good.\n\nPotential issue: \"Borrowed-mechanism constraint\" \"The six repositories mined for this plan carry an 'MIT with OpenAI/Anthropic Rider' license whose definition of use includes incorporation into automated pipelines. Cairn's standing policy — an operator ruling recorded here, not a conclusion derived from the license text — is to borrow mechanisms re-implemented from the primary paper or spec each brief cites and never to vendor code from, or depend on, those repositories. Every borrowed mechanism is small (tens to a few hundred lines) and textbook, so this costs little.\" Good.\n\nPotential issue: Build order M0 \"Done when: it runs the exemplar skill on a 40-bit toy curve; a deterministic M0 fixture (an integration fixture, not a second research skill) derives x and Q = xP from that output; the subprocess verifier accepts that pair, rejects a one-coordinate and a one-scalar negative fixture, and classifies a forced backend crash as FAIL; and the generator output, derivation receipt, verifier result and gate-run record are hashed, self-tested, cost-tagged substrate nodes.\" Good.\n\nPotential issue: M0 exemplar skill \"toy_curve(bits, seed) → (p, a, b, n, P)\" \"accept if the order is prime (acceptance ≈ c/ln p, so expected tries grow like ln p with a geometric spread whose sd equals its mean; observed draws span 20–220 at 30–50 bits and are not a profile — M0 measures the constant over ≥ 50 seeds per size before declaring one)\" Good.\n\nPotential issue: \"Tier-0 verifier: subgroup membership (n·Q = O) then xP == Q, in a subprocess with decimal-string inputs and a one-line result; PARI's generic discrete log can loop when no solution exists, so membership precedes any log call. Acceptance is affirmative, never residual: the driver validates arity and every field before spawning (the backend zero-fills missing arguments), passes an explicit stack ceiling (60-bit ellcard overflows PARI's default), and accepts only exit == 0 and stdout == \"OK\" and empty stderr — a zero exit code is not evidence of anything, since the backend exits 0 on fatal errors including stack overflow.\" Good.\n\nPotential issue: M1 \"Done when: every fixture in the M1 planted corpus is caught under a cold cache (skip_cache_lookup), INCONCLUSIVE counting as an escape, and no matched true advance is rejected; minimum corpus, ≥ 30 plantings across it: (a) ... (i) a non-reproducible measurement.\" Good.\n\nPotential issue: M2 \"Done when: a parked branch auto-revives on blocker-clear; a refuted one is never re-walked.\" Good.\n\nPotential issue: M3 \"Done when: on ≥ 20 seeded runs each containing a stalled branch — a branch whose gate-outcome yield over the last k ticks is indistinguishable from zero — funding is withdrawn from it within m ticks unaided, with k and m pinned in the M3 test plan. Decides the allocation mechanics and crash-safe tick shape (§15 P3, P6) against that bar, including whether a bandit earns its place at all.\" Good.\n\nPotential issue: M4 \"Done when: multi-track portfolio runs with ledger + calibration coherent, and the selector's calibration is being tracked.\" Good.\n\nPotential issue: \"Anti-over-engineering\" Good.\n\nPotential issue: \"Engineering tracking. The harness engineering (M0–M4) is converted to beads with dependency edges ... and closed under /beads-compliance-and-completion-verification; the research tree itself is never pre-decomposed into beads — it is built and rewritten at runtime by the orchestrator.\" Good.\n\nPotential issue: \"Honest expected outcomes\" Good.\n\nPotential issue: \"Decisions deferred to a prototype\" P1-P7. Need maybe evaluate.\n\nP1: \"A pre-registered betting e-process on sampled instances: the Experimentalist declares a sampling distribution D over the family, ε, α and a seed before sampling (all hashed into the claim, so a new D is a new claim and the orchestrator cannot tune D toward clean cases), and accumulates E_t = ∏(1 − λ_t(X_t − ε)), λ_t ∈ [0, 1/(1−ε)], against H₀ 'P fails with probability ≥ ε under D'; Tier-2 spend on proving the conjecture requires E ≥ 1/α.\" Need check e-process formula. For testing H0: E[X] ≤ ε? Actually if X_t is indicator of success? They want \"P fails with probability ≥ ε\" under D. If X_t = 1 if failure? Then under H0, E[X] ≥ ε. To test E[X] ≤ ε? The e-process for H0: E[X] ≥ ε would use betting against? Let's derive. If H0: P(failure) ≥ ε, then X_t indicator failure has mean ≥ ε. We want evidence against H0 (i.e., failure rate lower than ε) to justify proving conjecture. An e-process for H0: E[X] ≥ ε can be E_t = ∏(1 + λ_t(X_t - ε))? Wait if H0 is E[X] ≥ ε, then for any λ ∈ [0, 1/(1-ε)]? Let's check. If X ∈ {0,1}, E[1 + λ(X-ε)] = 1 + λ(E[X]-ε) ≥ 1 if E[X] ≥ ε and λ ≥ 0. So it's an e-process. The plan writes E_t = ∏(1 − λ_t(X_t − ε)), λ_t ∈ [0, 1/(1−ε)]. That's same as 1 + λ(ε - X) = 1 - λ(X-ε). If λ ≥ 0, E[1 - λ(X-ε)] = 1 - λ(E[X]-ε) ≤ 1 under H0, not ≥ 1. So it's not an e-process; it's a test supermartingale? Actually for H0 E[X] ≥ ε, 1 - λ(X-ε) has expectation ≤ 1, so it's a supermartingale, and product is a supermartingale, not an e-process. To get an e-process, use 1 + λ(X-ε) with λ ∈ [0, 1/(1-ε)]? Let's check if λ can be >? If X=1, factor = 1+λ(1-ε) ≤ 1+1/(1-ε)*(1-ε)=2. If X=0, factor = 1-λε ≥ 1-ε/(1-ε) which can be negative if ε>0.5. Need λ ≤ 1/ε? Actually to keep nonnegative, λ ≤ 1/ε for X=0. So λ ∈ [0, min(1/(1-ε), 1/ε)]? For ε small, 1/(1-ε) ≈1, 1/ε large, so λ ≤ 1/(1-ε). For ε > 0.5, λ ≤ 1/ε. The plan's λ ∈ [0, 1/(1−ε)] is wrong for ε > 0.5? But ε is \"hundreds to thousands of clean cases\" so ε small. Still formula sign is wrong. This is a real technical error. Need propose fix: E_t = ∏(1 + λ_t(X_t − ε)) with λ_t ∈ [0, 1/(1−ε)] (and if ε > 0.5, also λ ≤ 1/ε; but for small ε okay). Or use E_t = ∏(1 + λ_t(ε - X_t))? Let's be precise. They want to reject H0 \"P fails with probability ≥ ε\" when observed failures are rare. If X_t = 1 if failure, H0: E[X] ≥ ε. An e-process for H0 is any nonnegative process with E[E_t | past] ≤ 1 under H0. For a single bet, choose factor f(X) with E[f(X)] ≤ 1 under all distributions with mean ≥ ε. The optimal f is f(1)=? f(0)=? Need f(1) ≤? Let's solve. Let p = P(X=1) ≥ ε. Need p f(1) + (1-p) f(0) ≤ 1 for all p ∈ [ε,1]. This is linear in p, so need at p=ε and p=1. At p=1: f(1) ≤ 1. At p=ε: ε f(1) + (1-ε) f(0) ≤ 1. To maximize evidence when p low, set f(0) large, f(1) small. Let f(1)=0, f(0)=1/(1-ε). Then factor = (1-X)/(1-ε). This is an e-variable: if X=0, 1/(1-ε); if X=1, 0. Product is 0 if any failure, which is too aggressive. More generally f(X) = 1 + λ(ε - X)? Let's compute: f(1)=1+λ(ε-1)=1-λ(1-ε), f(0)=1+λε. Need f(1)≥0 => λ≤1/(1-ε). Need E[f] = 1 + λ(ε - p) ≤ 1 for p≥ε. Yes! So f(X)=1+λ(ε-X) is a supermartingale? Wait E[f] = 1 + λ(ε - p) ≤ 1 under H0. That's a test supermartingale, not an e-process? An e-process is a nonnegative supermartingale with initial 1, so it is an e-process? Actually e-process is a nonnegative process that is a supermartingale w.r.t. every distribution in H0, and E[E_t] ≤ 1. So yes, it is an e-process. The plan's formula E_t = ∏(1 − λ_t(X_t − ε)) = ∏(1 + λ_t(ε - X_t)) is correct! I misread. It is an e-process. Good. Need not revise. But check λ range: λ ∈ [0, 1/(1−ε)] ensures f(1)≥0. Good. So P1 is fine.\n\nP2: \"Selector audit as a calibration test martingale (M4). Panel greenlight probability q_t vs gate-adjudicated outcome Y_t; M_t = ∏(1 + λ_t(Y_t − q_t)) with predictable stakes λ_t ∈ [−1/(1−q_t), 1/q_t], recorded before Y_t is observed, is a test martingale under 'calibrated' (each factor is nonnegative with conditional mean one only inside that range); with outcome lag the stopping rule needs the lagged correction, and the filtration under which q_t, λ_t and the adjudication become known is written down.\" Need check. If Y_t ∈ {0,1}, q_t = P(Y=1) under calibrated. Factor 1 + λ(Y - q). For nonnegative, need 1 - λ q ≥ 0 => λ ≤ 1/q, and 1 + λ(1-q) ≥ 0 => λ ≥ -1/(1-q). So λ range correct. E[factor] = 1 + λ(E[Y]-q) = 1 under calibrated. Good. Fine.\n\nP3: \"reward computed only from gate outcomes, with a relative-improvement baseline and a cost-aware blend as the nearest prior for info-per-dollar\" Good.\n\nP4: \"split-conformal anomaly flag on ladder and self-test measurements (order-statistic threshold returning +∞ when the sample is too small rather than clamping) and a one-sided bounded-mean e-process on log(measured/declared cost). Strategy-layer alarms only.\" Good.\n\nP5: \"Conformal surprise per claim class as an extra scrutiny trigger (M2). A claimed effect in the top-α of historical claims of its class raises scrutiny; it can never lower it.\" Good.\n\nP6: \"Exact-dispatch replay receipt for the orchestrator's own concurrency (M3). A sealed, config-bound record of every dispatch with typed mismatch errors on replay; only needed if the orchestrator is itself concurrent.\" Good.\n\nP7: \"Librarian retrieval stack and off-policy evaluation (M4). Hybrid BM25 + one embedding model fused by RRF (k ≈ 10; never two ensembled embedders), conformal retrieval depth as an advisory budget; IPS/DR off-policy evaluation of a proposed allocation policy from logged outcomes and propensities before switching.\" Good.\n\nPotential issue: \"SQLite + BLAKE3 as M0 store\" but \"off-the-shelf content-addressed caches ... their semantics are adopted, their binaries are not.\" Good.\n\nPotential issue: \"The ledger and branch-tree history are an append-only, hash-chained log (chain = H(domain ‖ prior_chain ‖ canonical_record), every variable-length field length-prefixed) with compare-and-swap head advancement, re-verifiable from origin; an orchestrator that rewrites history produces a log that fails verification.\" But \"orchestrator may rewrite the branch tree\" and \"branch-tree history are append-only\" seems contradictory. The tree is mutable, but history of tree edits is append-only. Good.\n\nPotential issue: \"Dead-ends become structural, not memory-dependent. When the orchestrator proposes a branch whose hypothesis key matches a REFUTED ledger entry, the system recognizes it instantly — same hypothesis, same key, already dead — with no agent needing to remember that two approaches are secretly the same.\" But \"near-duplicate signal ... advisory only\" because paraphrase. Good.\n\nPotential issue: \"Hypothesis key ... over a typed hypothesis object {target family, claimed property or cost model (exponent, constant, crossover — or the claim statement hash of §4), method identity (skill@version and parameters, or the statement hash of the approach), declared parameter ranges, declared sampling distribution D where one exists}. Per-run seeds and ladder instances are excluded, so two honest attempts at one hypothesis collide; free-text fields are excluded, so the key is about structure — paraphrase is the near-duplicate advisory's job (§4).\" Good.\n\nPotential issue: \"A measured refutation reaches exactly the hypothesis object it was measured on, because that object is what was hashed; a broader blacklist needs a formal entry.\" But if hypothesis key includes \"declared parameter ranges\", a measured refutation on a subset of declared range blocks the whole declared range. Maybe \"exactly the hypothesis object\" means the declared object, not actual measured. Could be okay. But maybe a worker can declare a narrow range to avoid blocking broad, then later broaden? That's a new hypothesis key, so not blocked. Fine.\n\nPotential issue: \"Retention: keep lineage + certificates forever (cheap, load-bearing); GC the giant intermediate blobs once their derivation is recorded and the result verified. Keep the edges, prune the mass.\" \"Mechanism: recipes, ledger rows, certificates and recorded divergences are GC roots; liveness is reachability from roots over lineage edges; only blobs are collectable.\" Good.\n\nPotential issue: \"Store: SQLite (blobs, lineage edges, roots table) keyed by BLAKE3 at M0.\" Good.\n\nPotential issue: \"Every skill ships a known-answer self-test.\" \"Skills that produce floats declare a numeric profile or a tolerance.\" \"Every corpus declares its origin ... author_supplied alone caps the skill's results at CONJECTURE — STRONG-EMPIRICAL needs an upstream corpus, an independent implementation under the axis rule below, or a postcondition or metamorphic relation checked on inputs drawn from a seed committed after the implementation revision is content-addressed.\" Good.\n\nPotential issue: \"Tier-0 arithmetic skills verify their own answer before returning (the randomized postcondition; Sage's discrete_log checks base^res == a before it returns).\" Good.\n\nPotential issue: \"Where a cross-check is claimed the skill declares its axis — implementation (distinct codebases) or algorithm (distinct methods in one codebase) — and the input range over which that axis is independent; outside that range it declares no cross-check and its result is capped accordingly. Two builds of one library (gp and cypari2 both wrap libpari) are one implementation, and a cross-check that has never disagreed is reported as untested, not as passing. On disagreement the skill returns status = DISAGREE (not OK, so never cached), writes both transcripts and digests to a substrate node, and the caller must park or refute; a silent retry with a third tool is forbidden.\" Good.\n\nPotential issue: \"Granularity rule: grain a skill at 'a capability with a stable interface, a self-test, and a declared cost profile' (§5) — no finer. Too fine and the orchestrator burns its budget gluing skills; too coarse and they stop being reusable.\" Good.\n\nPotential issue: \"Content-addressed substrate\" \"Every recipe is hashed by (skill@version, inputs-by-hash, seed, tool-versions, container-digest, salt).\" \"Key construction: BLAKE3-256 over a canonical, typed, domain-separated encoding of that tuple ... enforced at write time and covered by a known-answer test vector.\" Good.\n\nPotential issue: \"Cache bits on every node: do_not_cache (never memoize), skip_cache_lookup (run even if cached and record a fresh attempt — never overwrite; the Skeptic's re-runs always set it), status ≠ OK ⇒ never cached (a failure is a record, not a memo), and the salt key field that moves a whole class of results into a fresh namespace when a tool version is found faulty. For a Replayable recipe, two successful attempts with different output manifests mark the recipe non-reproducible and inadmissible, both attempts kept and rooted; for a Verifiable recipe each witness is its own attempt and must pass its verifier. A cached result is served only if every blob it references is present; otherwise it is recomputed.\" Good.\n\nPotential issue: \"Replayable ≠ verifiable.\" \"Replay grades: every node carries replay ∈ {Replayable (bit-identical re-run), Verifiable (a witness checked by a deterministic verifier), AuditOnly (logs only)}, strongest first. The grade can only be weakened, and AuditOnly is inadmissible as evidence.\" Good.\n\nPotential issue: \"Certificate = declared content address + witness. For a rho/kangaroo run the witness is the curve, n, P, Q, the walk definition (multipliers or their PRNG seed, index function, distinguished-point predicate θ) and the two colliding DP triples (X, a, b), (X, c, d); the verifier checks aP + bQ = X = cP + dQ, rejects b ≡ d (mod n) outright (never retries silently), computes x = (a − c)(d − b)⁻¹ mod n and checks xP == Q. Storing seeds instead of (a, b) shrinks reports but makes verification cost O(1/θ) walk steps; that is a Tier-3 storage optimization, not the certificate. A certificate certifies identity and checkability, never truth: the artifact keeps its producer's calibration tag.\" Good.\n\nPotential issue: \"Reproducibility gate = re-run and compare. Admission re-runs the recipe (or re-checks the witness) and compares bitwise; on divergence the second copy is kept, rooted, as evidence of non-reproducibility and the node is marked inadmissible. The re-run policy is fixed by grade and tier and budgeted before launch: Verifiable ⇒ the witness is always checked; Replayable at Tier 0/1 ⇒ always re-run; Replayable at Tier 2/3 ⇒ re-run at the moment a promotion past CONJECTURE is sought (the promotion waits on it), otherwise sampled by the foundations auditor (§7) with the claim's tag capped at CONJECTURE meanwhile.\" Good.\n\nPotential issue: \"The tag is derived, never set.\" \"A weaker class may inform a stronger claim; it may never justify it. The foundations auditor's core query is the transitive form: no claim, of any class, may carry a justified_by edge at any depth to a strictly weaker premise, and a failed re-verification downgrades the node and re-derives every claim transitively justified by it.\" Good.\n\nPotential issue: \"Statement-level review: Lean verifies a proof is valid, not that you stated the theorem you meant. Verified-but-wrong-statement (weaker/trivial, smuggled hypothesis, wrong quantifier) is more dangerous than an open gap. Check the statement separately.\" \"Mechanical pre-filters (reject or flag, never pass): a vacuity check that tries to derive False from the hypotheses (certified vacuous ⇒ reject); linters for some forms of the ∃ x, P x → Q trap (binder-predicate and conjunction-nested forms pass them silently, so the Skeptic's checklist still carries that item), stubs and new axioms; a bounded prove/disprove attempt with a weak prover (trivially provable or disprovable ⇒ flag); a blind round-trip informalization diffed against the source claim (divergence ⇒ flag). The Skeptic's rubric is the quantifier-order / strict-vs-non-strict / implication-direction / domain-and-boundary checklist (ZMod 0, x/0, sInf ∅, impossible hypotheses). The human verdict remains the gate.\" Good.\n\nPotential issue: \"Independent skeptic: sees only the statement, never the prover's reasoning; rewarded solely for gaps/counterexamples. If it reads the proof it just agrees — independence is the whole value.\" \"By construction, at both layers. Context: the skeptic is dispatched by the harness as its own top-level query whose entire prompt is the statement node, with inherited settings sources disabled — never composed by a parent model. Capability: an explicit tool allow-list for counterexample search, no general file read, no shell, and a substrate handle scoped to the statement node's closure, so no prover output, panel feedback or gate rubric is reachable. Both are gate-bundle config (§4), and the dispatch record names the allow-list it ran under, so a widened one is visible in the ledger. Its re-runs of any experiment set skip_cache_lookup (§3).\" As noted, re-run needs recipe. Need maybe clarify.\n\nPotential issue: \"Disagreement protocol: a Prover/Skeptic or panel disagreement is never resolved by vote; it is recorded with both artifacts' hashes, classified (statement error, proof gap, harness bug, out-of-scope, inconclusive), routed to an owner (the human for statement errors), and the claim's tag stays at its pre-dispute level until resolved.\" Good.\n\nPotential issue: \"Proportional scrutiny + the paradox, written in: tightened bound → clean argument; claimed break of the target → formalization + ladder + expert sign-off. State it in the orchestrator: a run announcing it broke ECDLP has produced evidence it erred. The bigger the claim, the more the burden inverts against it.\" Good.\n\nPotential issue: \"Strong law of small numbers (hard gate): a small-case pattern is a conjecture until it survives larger cases + a deliberate counterexample hunt.\" Good.\n\nPotential issue: \"Standing foundations-auditor (above the per-claim skeptic): the per-claim skeptic catches bad proofs; it can't catch a bad premise the whole tree rests on (an FFDA-style heuristic everyone assumed). Periodically re-audit the shared assumptions — a tall tower on a cracked base is the failure no local check sees. Its mechanical core is the transitive justified_by query above; its sampling cadence for re-verifying substrate nodes uses the spot-check bound l ≥ ln(2^bits)/ln(1/(1−f)) so that undetected rot at fraction f has probability below 2^−bits; f and bits are named in the gate bundle (M0: f = 0.01, bits = 20), the bound is applied per tier stratum so that cheap Tier-0/1 nodes cannot absorb the whole sample, and within the Tier-2/3 stratum the nodes with the largest justified_by fan-in are drawn first. The re-run it performs is the §3 policy; what it audits is every claim's evidence, not a report.\" Good.\n\nPotential issue: \"No-go checklist\" \"Any proposed attack on the target must state how it evades all three or be flagged almost-certainly-broken.\" \"The three: (1) Shoup √n — must exploit named curve-specific structure, not generic ops; (2) isogeny invariance — order & embedding degree are invariant, walking can't help; (3) prime-field index-calculus obstruction — no efficient point decomposition over prime fields; Weil-descent/GHS/summation-polynomial methods need F_{q^n}, n>1.\" Good.\n\nPotential issue: \"Never give up\" Good.\n\nPotential issue: \"Problem selection, taste & the human loop\" \"Two non-LLM anchors: (a) cheap empirical reality — a candidate must yield a Tier-0/1 testable prediction before 'the models like it' counts; (b) a human ruling at the meta level (the reasons above are checkable without understanding the proof), with a domain expert signing off on expensive, high-commitment calls. When no human session is open, a gate input that requires a human (expert sign-off, a statement-review verdict, a waiver) is absent and the gate blocks: the claim waits at its pre-review tag and surfaces on the human queue; absence never defaults to pass. Whether Tier-2 spend also requires an open operator session is an operator knob in the gate bundle, not a default this plan sets.\" Good.\n\nPotential issue: \"The selector is itself audited.\" Good.\n\nPotential issue: \"Legible state\" Good.\n\nPotential issue: \"Compounding assets\" \"Formalized-lemma library (shared, mathlib-style): every PROVEN lemma lands here, so month N is cheaper than month 1 — deeper scaffolding is what 'accretive' means.\" \"Coverage decision (mathlib @1f29011, toolchain v4.34.0-rc1): adequate for statements about curve groups and generic-group/DLP lemmas — Weierstrass curves, the proved affine group law, division polynomials, finite fields are present. First library items, absent upstream: Finite W.Point over a finite field, the Hasse bound, a DLP definition (∃ k, k • P = Q), Gröbner bases as library objects, summation polynomials. Intake runs the §","ms":162933}],"ok_count":4,"exit":0}
